# Project State — Nimbus Storage Platform

Status: current as of the Vercel frontend deploy (2026-08-09, item 44)
This is the authoritative "what's actually true right now" snapshot. If any other doc in `docs/` disagrees with this one, this one wins — flag the drift and fix the other doc.

## What Nimbus is

A self-hosted, distributed cloud storage platform (Dropbox/Drive-alike) built as a portfolio-quality demonstration of distributed-systems engineering. Go modular-monolith backend + one extracted worker service, Next.js frontend, Docker Compose for local infra. Three deliberate "standout" features, all built and verified: consistent-hash-routed multi-node storage with replication/failover, chunked/resumable/deduplicated upload, event-driven NATS processing (thumbnails).

## Architecture (as built)

- **`nimbus-api`** (Go, `backend/cmd/api`): modular monolith. Domain modules under `backend/internal/`: `auth`, `org`, `folder`, `file`, `upload`, `storage`, `sharing`, `search`, `activity`. Cross-module boundaries enforced via small interfaces satisfied by adapters in `main.go` — no module reaches into another's Postgres tables.
- **`nimbus-worker`** (Go, `backend/cmd/worker`): separate binary, same `go.mod`, imports `internal/` packages as a library (not network calls). Subscribes to NATS JetStream, reassembles chunks, generates thumbnails, writes activity. Runs its own `storage.Router` + health-check loop (health state is in-process, not shared).
- **Frontend** (`frontend/`): Next.js 16.3.0, App Router, TypeScript, Tailwind v4, SWR. Auth tokens: the access token lives in memory only (`lib/tokens.ts`); the refresh token is an `httpOnly`/`SameSite=Strict` cookie the app's own `app/api/auth/*` route handlers (a thin BFF proxy, item 31) set and read — never JS-readable. Runs via `npm run dev` for day-to-day dev; also containerized as of Day 13 (`deploy/Dockerfile.web`, standalone output) for Compose and Kubernetes.
- **Infra** (Docker Compose, `deploy/docker-compose.yml`): Postgres, Redis, NATS, 3× standalone MinIO nodes, `nimbus-api`, `nimbus-worker`, `nimbus-web`, `prometheus`, `grafana`, `mailpit` (SMTP catcher for password-reset email, Tier 4 — UI on host port 8025, SMTP on 1025). Both app processes expose `/metrics`; Prometheus scrapes both, Grafana auto-provisions its Prometheus datasource plus two dashboards from `deploy/observability/grafana/` on container start.
- **Kubernetes** (`deploy/k8s/`, Day 13): `helm/nimbus/` is the chart owned by this project (api/worker/web); `infra/` is plain manifests for stateful deps (Postgres/Redis/NATS/MinIO×3/Prometheus/Grafana). Deploys to a local `kind` cluster (`deploy/k8s/kind-config.yaml`) with NodePorts mapped to the same host ports Compose uses — the two aren't meant to run simultaneously.
- **Storage routing**: SHA-1 hash ring, 128 vnodes/node, Dynamo-style preference list, N=2 replication / W=2 write quorum, 2-state circuit breaker per node (closed/open — no half-open; the 2s health-check cadence already serves as the retry trial).
- **Upload**: client-side SHA-256 chunking (8 MiB default), presigned PUT direct to MinIO, dedup via `/chunks/check`, cross-replica ETag verification on commit.
- **Auth**: JWT HS256 (15 min) + rotating opaque refresh tokens (7 day, reuse-detection revokes the whole family), Redis-backed access-token blacklist for logout. Since Tier 4: email password reset (single-use hashed tokens, 1h TTL, SMTP via Mailpit — or link-logging when `NIMBUS_SMTP_ADDR` is unset) and optional TOTP 2FA (RFC 6238 on the Go stdlib, Redis challenge + per-step replay guard, two-step login).

## Completed features (Days 1-10, all verified against a real running stack)

1. Repo scaffold, Docker Compose skeleton, config/logging/http platform packages, migrations, CI skeleton (lint+build only).
2. Auth (register/login/refresh/logout, rotation) + orgs/membership.
3. Folder + file metadata CRUD, trash/restore.
4. Storage module: hash ring, health checks, `/v1/admin/nodes`, failover.
5. Chunked/resumable/deduplicated upload.
6. Download-plan + version history/restore.
7. Public share links with ACL/expiry.
8. Full-text search (Postgres tsvector) + activity feed + NATS publish on upload-complete.
9. `nimbus-worker`: thumbnail generation (image + PDF placeholder), async activity writes.
10. Full Next.js frontend: auth, org/folder browser, drag-drop chunked upload with progress, file preview, sharing UI, trash UI, activity feed, admin node-health page. CORS added to the backend to support it.
11. Prometheus instrumentation on `nimbus-api` and `nimbus-worker` (`/metrics`, HTTP histogram by route pattern, upload throughput, storage placement failures, per-node health gauge, NATS consumer lag) + `prometheus`/`grafana` added to Compose + two auto-provisioned Grafana dashboards (golden signals, storage health). See docs/03-hld.md §2.
12. `scripts/chaos-node-kill.js`: full mid-upload chaos scenario (kill a node after 2 of 7 chunks commit, assert remaining placement avoids it, upload completes, download checksum-matches, node recovers) — 10/10 assertions pass on a real run. Targeted Go integration tests (`internal/auth` refresh-reuse-revokes-family, `internal/upload` concurrent-complete race) against real Postgres/Redis, gated behind `-tags=integration`. `scripts/load-upload.js` (k6): 60 concurrent VUs driving the real chunked-upload flow, 3467 uploads, 0% failures — proves NFR-2. See docs/07-distributed-architecture.md §5.
13. Frontend containerized (`deploy/Dockerfile.web`, Next.js standalone output) + added to Compose. `deploy/k8s/infra/` (plain manifests: Postgres/Redis/NATS/MinIO×3/Prometheus/Grafana) + `deploy/k8s/helm/nimbus/` (chart we own: api/worker/web, ConfigMap/Secret, HPA stub, migrate Job as a Helm pre-install hook). Deployed to a local `kind` cluster and verified live: migrations ran (16 tables), a full chunked upload completed via real presigned MinIO URLs reachable through kind's NodePort mappings, dedup worked, the worker/NATS thumbnail pipeline produced a `thumbnail_key`, and both Prometheus targets + both Grafana dashboards came up automatically. See docs/03-hld.md §3.
14. CI hardened: `frontend` job (npm lint+build) and `integration-test` job (real Postgres/Redis service containers, runs the `-tags=integration` suite) added; `docker-build` extended to all four images (api/worker/web/migrate). Chaos/load tests deliberately stay out of CI (scope decision, see "Known issues" below). README got the Mermaid architecture diagram and a numbered demo script, and now documents both the Compose and kind paths. See docs/09-roadmap.md Day 14.

Plus, inserted mid-plan (not in the original roadmap): repo restructured from a single tree into top-level `backend/` + `frontend/` siblings, at the user's request, ahead of Day 10.

15. SRS Definition-of-Done pass (buffer day, no new features beyond closing two flagged gaps): built FR-4 (`auth_audit_log` table in `internal/auth`, best-effort recorded on login/refresh/logout, verified end-to-end against the live Day 13 `kind` cluster — register→login→refresh→logout produced exactly the 3 expected rows) and FR-8's read-side verification (`processing.Processor.reassemble` now re-hashes each fetched chunk against its content-addressed hash before accepting it, falling back to the next replica on a mismatch — verified against real corrupted MinIO replicas: MinIO's own bitrot detection surfaces corruption as a read error before our hash check even runs, our code correctly falls back and, when all replicas are bad, correctly errors out for NATS redelivery rather than silently serving/skipping). FR-26 was explicitly narrowed (per-org usage stays a documented v1 gap, not built). NFR-4 was measured for the first time: `scripts/load-metadata.js` (new), p95 20.5ms at 60 concurrent VUs against `GET /v1/orgs/{orgId}/folders` — comfortably under the 200ms bound. NFR-6 gained actual enforcement: GitHub branch protection added on `main` requiring the 4 CI jobs to pass before a PR can merge (direct pushes still work; force-push/delete are blocked). `docs/06-api-design.md` was checked route-by-route against `main.go` and found accurate (no drift) — just bumped its status line and added a one-line note about the new (API-invisible) audit log. README's "steps 2-6 work identically" claim was found to be wrong for step 3 (`chaos-node-kill.js` is hardcoded to `docker compose stop/start`, no kind equivalent) and corrected. See docs/01-srs.md §8 for the full DoD-pass writeup.

16. (Post-roadmap, same day as 15) Frontend visual overhaul + first cloud deployment: the entire Next.js frontend was restyled to the **Dashdark X** design language (user-picked Figma community file; tokens extracted verbatim from its Figma variables — see `frontend/app/globals.css`), made fully responsive (desktop sidebar ≥lg; sticky top bar + bottom tab bar below that), and verified live at 375px/1280px plus a full UI-driven workflow test (upload → share → version history → activity, all through real clicks). The **frontend (only) is deployed to Vercel**: project `nimbus-storage-platform`, production alias `https://nimbus-storage-platform.vercel.app`, deployed via the `vercel` CLI from `frontend/` (a `.vercel/` link dir + an OIDC token in `frontend/.env.local` exist locally, both gitignored). It uses `NEXT_PUBLIC_API_URL`'s default `http://localhost:8080`, so it is fully functional only from a machine running the backend stack locally; for anyone else it's a UI showcase with failing API calls (expected, documented). Backend stays local (kind/Compose) — see "Known issues" for the go-public plan.

17. (Tier 1 backlog session, 2026-07-05) All six Tier 1 backlog items (docs/next-session.md #1–#6) built and verified against a live Compose stack via real browser interaction: **thumbnails in the UI** (new `GET /v1/files/{fileId}/thumbnail` returning presigned GETs in ring-preference order healthy-first — a thumbnail's node isn't recorded anywhere, the worker stored it on the first healthy node of that same deterministic list, so the client walks the targets like a download plan; new `storage.Router.Candidates` gives the unfiltered preference list; children listing now carries `size_bytes`/`mime_type`/`has_thumbnail` per file; FileRow renders the thumb + click-to-preview modal), **org members UI** (`/app/org/{orgId}/members`, invite-by-email + role pills + remove, owner-gating surfaced as readable 403 messages rather than guessed client-side; nav now has 6 items), **move files/folders UI** (shared `MoveDialog` drill-down folder picker; hides the folder being moved, which alone keeps the picker cycle-free), **search filters + pagination** (type/owner/date/size filter panel, removable filter chips that re-run the search, cursor-based "Load more"), **share expiry** (1h/1d/7d/never dropdown wired to `expires_at`; expiry verified end-to-end by forcing a link past-expiry in Postgres and seeing the public share page refuse it), and **breadcrumbs** (new `GET /v1/folders/{folderId}/path` recursive-CTE ancestors endpoint + trail replacing the old jump-to-root link).

18. (Tier 2 backlog session, same day, 2026-07-05) All three Tier 2 backlog items built and verified against the live Compose stack: **upload caps + org quotas** (`NIMBUS_MAX_UPLOAD_BYTES` default 100 MiB → 413 `too_large`; `NIMBUS_ORG_QUOTA_BYTES` default 10 GiB → 507 `quota_exceeded`; enforced at upload-init *and* re-checked at complete since nothing forces the two declared sizes to agree; usage = `file.Repository.OrgUsageBytes`, logical bytes over every stored version incl. trashed — purge frees quota, dedup deliberately doesn't discount it; verified with a 1 MB quota override → real 507), **rate limiting** (`internal/platform/ratelimit`: Redis token bucket as an atomic Lua script, default 25 rps/burst 50, keyed per-user via new signature-only `auth.Service.PeekUserID` with per-IP fallback, health/metrics endpoints exempt, fails open on Redis errors; verified with a 2 rps/5-burst override → exact token-bucket 429 pattern incl. mid-loop refill, separate user-vs-IP buckets proven, `Retry-After` header present), and **DLQ visibility** (a message failing its final NATS delivery is now recorded in the new `dead_events` table — migration 000006 — with payload/error/delivery count, then Term'd; `GET /v1/admin/dlq` + `POST /v1/admin/dlq/{id}/retry` republish the stored payload; admin page grew a Dead-letter queue panel with a Retry button; verified genuinely end-to-end: deleted a committed chunk from all 3 MinIO nodes before `/complete`, watched the event exhaust 5 real deliveries into `dead_events`, restored the chunk bytes, clicked Retry in the UI, and the worker generated the thumbnail on the republished event). Note the DLQ design **revision**: Postgres table + inline final-delivery detection, not the `nimbus.uploads.completed.dlq` subject docs/07 originally sketched — docs/07 §3 records why.

19. (Tier 3 backlog session, 2026-07-05) All four Tier 3 backlog items built and verified against the live Compose stack: **chunk garbage collection** (#10 — migration 000007 adds `chunks.gc_state/last_seen_at/doomed_at` + the `file_version_chunks(chunk_hash)` refcount index; `internal/gc` mark/sweep in nimbus-worker with a dedup-lease on `/chunks/check` (touch `last_seen_at`, doomed reads as missing) and commit-time resurrection; per-chunk sweep transactions with `FOR UPDATE` re-verify, objects-then-row deletion, at-least-once retry when a replica node is down; full design incl. the documented residual race in docs/07 §6; `NIMBUS_GC_INTERVAL`/`NIMBUS_GC_GRACE` config; 4 new `nimbus_gc_*` metrics), **trash auto-purge** (#11 — FR-11's retention window finally enforced: `PurgeExpiredTrash` on file/folder repos called from the GC tick via the `gc.TrashPurger` port; folder purge guarded by a subtree liveness check since its row delete CASCADEs), **SSE live updates** (#12 — `GET /v1/orgs/{orgId}/events` in new `internal/live`, relaying Redis pub/sub: a new per-org activity channel published by `activity.Service` (covers worker events too) + the existing `nimbus:health:changes`; frontend `useLiveEvents` hook (fetch-based, not EventSource — Authorization header) drives SWR revalidation on the admin/activity/folder pages — verified live: node kill flips red without reload, an API-side upload made the file row *and* its worker thumbnail pop into a watching browser), and **hash-ring visualization** (#13 — `GET /v1/admin/ring[?file_id=]` exposing the vnode table + a file's chunk positions/preference/recorded locations; admin-page SVG ring with per-node colors that dim live when a node dies, file search-and-plot flow). Verified by the new 13-assertion `scripts/smoke-gc.js` (dedup protection, doom, resurrection, physical sweep, auto-purge, folder guard, down-node retry) plus live browser testing. **Also fixed a real latent bug found by the GC suite**: migration 000008 — `uploads.file_id/version_id/target_file_id` FKs had no `ON DELETE` action, so purging any uploaded file (i.e. every file) 500'd with an FK violation; explicit purge had been silently broken since Day 6. Now `ON DELETE SET NULL`.

20. (Post-Tier-3 UX/sharing session, same day, 2026-07-05 — user-requested items, not from the numbered backlog) Four fixes/features, all verified live in the browser: **styled confirmation dialog** (new `ConfirmDialog` component replaces the native `window.confirm` "localhost:3000 says…" popup on the trash page's Delete forever; async-aware — shows a busy state and surfaces purge failures inline), **real PDF thumbnails** (`processing.generatePDFThumbnail` renders the first page via go-pdfium's WebAssembly runtime — pdfium compiled to WASM run under wazero, so `CGO_ENABLED=0` builds keep working; composited onto white before JPEG encode since PDF pages have no intrinsic background; the Day 9 solid-red placeholder survives only as the fallback for unparseable PDFs; WASM module warm-up runs at worker boot after the first live PDF was observed exceeding the NATS ack window during cold compile, causing redundant-but-idempotent redeliveries), **folder shares** (`POST /v1/folders/{folderId}/share` — public page lists the folder navigably, subfolders included, scope-checked by `IsSelfOrDescendant`), and **multi-file bundle shares** (checkbox multi-select in the folder view + "Share selected (n)" → `POST /v1/orgs/{orgId}/shares` — one link for the set instead of a link per file; a bundle of one normalizes to a plain file share). Share links are now scoped exactly one of file/folder/bundle (migration 000009: nullable `file_id`, new `folder_id` + `share_link_files` + backfilled `org_id` for one-step revoke auth); the public resolve response is discriminated by `kind`, with per-file download plans presigned on demand via `GET /v1/shares/{token}/files/{fileId}/download-plan` (scope violations 404 identically whether the file exists or not — no probing). Legacy single-file shares regression-checked via `scripts/smoke-sharing.js` (all green, shape unchanged).

21. (Tier 4 session, 2026-07-06) Both Tier 4 backlog items built and verified against the live Compose stack, plus one user-requested UX tweak: **password reset flow** (#14 — migration 000010 `password_reset_tokens` (hashed single-use tokens, 1h TTL); `POST /v1/auth/password/forgot` always answers 202 (no enumeration oracle) and emails a reset link via the new `internal/platform/mail` SMTP sender to the new `mailpit` compose service (UI on :8025) — with `NIMBUS_SMTP_ADDR` unset (e.g. kind) the link is logged instead; `POST /v1/auth/password/reset` consumes the token, rewrites the hash, and revokes **all** refresh families in one transaction; frontend `/forgot-password` + `/reset-password` pages and a "Forgot password?" login link; new config `NIMBUS_SMTP_ADDR`/`NIMBUS_SMTP_FROM`/`NIMBUS_WEB_BASE_URL`), and **TOTP 2FA** (#15 — migration 000011 `user_totp`; RFC 6238 HMAC-SHA1/6-digit/30s/±1-step implemented on the Go stdlib in `internal/auth/totp.go` with unit tests locked to the RFC's Appendix B vectors; enrollment is setup → QR/secret → confirm-with-code (pending enrollment doesn't gate login, confirmed one is never overwritten); login becomes two-step: password → 5-min Redis challenge → `POST /v1/auth/login/totp`; a Redis one-use-per-time-step guard blocks code replay across login/confirm/disable; disable requires a current code; new audit verbs `password_reset`/`totp_enabled`/`totp_disabled`; frontend Security page (7th nav item) with QR via the `qrcode` npm package, and a 2FA code step on the login page). Also: **share links now auto-copy to the clipboard on creation** (user request — FileRow single-file shares and ShareDialog folder/bundle shares both write the URL to `navigator.clipboard` immediately, with the manual Copy button kept as fallback for when the browser refuses the write). Verified by a 23-assertion scratchpad smoke (independent Node TOTP implementation agreeing with the Go one; real Mailpit email fished for the reset link; single-use, enumeration, replay, challenge-consumption and session-revocation cases) plus full live-browser flows: UI enrollment with a QR render, logout → two-step login with a computed code, UI forgot→email→reset→login-with-new-password (TOTP surviving the reset), and the share "Copied" state lighting up on a real click. A same-session follow-up added **"Download folder" on the public folder-share page**: recursively walks the share via the public children endpoint, fetches each file's presigned plan just-in-time (15-min expiry vs. big trees), reassembles chunks client-side, and packages a zip in the browser (`jszip`) — client-side on purpose, since the server never touches file bytes on the download path (FR-8). The button downloads whatever folder is on screen (whole share at the root, just the subtree inside one). Verified live: captured the emitted blob on a real share of the Tier 1 Pictures folder — valid zip magic, all 3 files + the `Vacation 2026/` subfolder path present. A second follow-up added the bundle-share equivalent — **"Download all" on the public bundle-share page** (`zipFilesInto`, same just-in-time per-file plan fetch as the folder version, but flat — a bundle has no subfolder structure to namespace names, so same-name files get a `(n)` suffix rather than silently colliding in the zip) — and fixed the multi-select checkbox styling in the app's file browser (Tier 1's bundle-share picker): the native checkbox's unthemeable browser-default rendering (a stark white square against the dark UI, `FileRow`'s only `accentColor`-only styling didn't touch the unchecked state) is now a custom `ui/Checkbox` component — native input hidden but still driving state/click-target/focus, a themed box shown instead (`surface-deep` + hairline border unchecked, the brand gradient + a check glyph when checked). Verified live: captured the "Download all" blob on a real bundle share (valid zip, all 3 files present) and screenshotted both checkbox states in the folder view.

22. (Governance session, 2026-07-06, after Tier 4 — user-requested after asking how roles work) Two access-control features, both verified live: **platform-admin gating on `/v1/admin/*`** (migration 000012 adds `users.is_platform_admin`; the cluster-ops routes — nodes, ring, DLQ list/retry — previously required only a valid JWT from *any* user and now require the flag via `auth.RequirePlatformAdmin`; granted at boot from `NIMBUS_PLATFORM_ADMIN_EMAILS` (promote-only — dropping an email never silently revokes; revoke is a manual UPDATE); compose promotes `tier1-demo@nimbus.dev`, `test@gmail.com`, `test1@gmail.com`; new `GET /v1/auth/me` tells the frontend who it's rendering for — the Admin nav item is hidden for non-admins and direct navigation shows a readable access card), and **owner-gated org oversight** (`GET /v1/orgs/{orgId}/usage` — the endpoint drafted in early API docs finally built, but under `/v1/orgs/` not `/v1/admin/` since the admin prefix now means cluster ops; assembled in `org` via ports over file/sharing/activity (`org/usage.go`): storage used vs quota + live/trashed file counts, active share-link count, per-member last-active + 30-day event count, org-wide 30-day verb breakdown. Privacy line documented in the code: aggregate action *metadata* already visible to members via the activity feed — no file names/content, and explicitly NOT `auth_audit_log`, which spans orgs and is user-private. Members page shows owners four stat cards + Last active / Actions (30d) table columns; plain members get a 403 the UI swallows — no panel, no error). Verified by a 21-assertion scratchpad smoke (admin 200s vs non-admin 403s on all four routes, 401-before-403 ordering, owner usage numbers, member 403, fresh-member appearing with zero activity) plus live browser checks of all three perspectives (owner+admin, member, member-navigating-to-/admin). `governance-demo@nimbus.dev` / `governance-demo-pass` was left in Tier1 Demo Org as a plain-member demo account.

23. (Governance session part 2, same day, 2026-07-06) The remaining two recommendations from the roles discussion, both verified live: **a delegated org-admin role tier** (migration 000013 adds `admin` to the `member_role` enum — ladder is now owner > admin > member, compared via `org.roleRank` in a now-rank-based `RequireRole`; admins get org governance — the usage view plus member management — under hard Service-enforced bounds: they can only *grant* `member` (`ErrElevatedRoleNeedsOwner`) and only *remove* plain members (`ErrAdminRemovesMembersOnly`), so a delegated admin can never consolidate control; the creator stays unremovable by anyone; file/folder/share access remains identical across all roles; member add/remove and `GET /v1/orgs/{orgId}/usage` are now `RequireRole(RoleAdmin)` with the owner-vs-admin distinction in `org.Service`), and **the cluster-vs-governance naming split in the UI** (nav item renamed "Admin" → **"Cluster"**, page header "Cluster operations" — infrastructure stops masquerading as org admin; the Members page gained an "Organization usage" section label and is now role-aware via `/v1/auth/me`: plain members see no invite form or remove buttons, admins see an invite form offering only the Member role and remove buttons only on plain members, owners see everything; the server still enforces every rule — the UI just stops offering actions that can only 403). Verified by a 15-assertion scratchpad smoke (owner grants admin; admin reads usage + invites members; admin 403s granting admin/owner and removing a fellow admin; 409 removing the creator; member floor unchanged) plus browser checks of all three role perspectives. Demo accounts now cover the full ladder: `tier1-demo@nimbus.dev` (owner + platform admin), `org-admin-demo@nimbus.dev` / `org-admin-demo-pass` (org admin), `governance-demo@nimbus.dev` (plain member).

24. (Default-org-on-signup session, same day, 2026-07-06 — user-reported bug: a fresh signup with a different login showed no orgs and no data) `auth.Service.Register` (`backend/internal/auth/service.go`) previously only created the user row, leaving every new account org-less until someone manually called `POST /v1/orgs` or was invited — exactly the empty-account confusion reported. Register now takes an optional `org_name` and, after creating the user, calls a new `auth.OrgCreator` port to create a default org owned by that user (name from the request, or `"{email-local-part}'s Organization"` if left blank) — mirroring the existing best-effort, non-fatal pattern `org.Service.Create` already uses for its default root folder, so a failure here is logged rather than failing registration outright (the user row already exists by that point). `org.Service` gained `CreateForOwner` to satisfy the port directly (no adapter needed); `cmd/api/main.go` reorders wiring so `org.Service` is built before `auth.Service` since the latter now depends on it. Frontend: `/register` gained an optional "Organization name" field, threaded through `lib/auth-context.tsx` and `lib/api.ts`. Verified live against the rebuilt Compose `nimbus-api` image: signup with an explicit name ("Rocket Labs") and with a blank name (defaulted to `newuser2's Organization`) both produced a real org + owner membership + default "Home" folder, confirmed via `GET /v1/orgs` and a full browser walkthrough showing the org name in the header and a working empty file browser.

25. (Pre-deployment audit session, same day, 2026-07-06 — user asked for an extreme-detail pass over the whole app before going public) Ran a three-angle audit (security/secrets, deployment config, correctness of the just-added default-org-on-signup feature) via parallel review agents, verified the top findings by hand, then fixed everything actionable now rather than deferring it to the VPS session:
    - **Single seeded platform-admin login replaces `NIMBUS_PLATFORM_ADMIN_EMAILS` entirely** (user-requested, closing a real gap the audit flagged: the old email-list mechanism meant anyone who ever signed up with `test@gmail.com` on a public instance would auto-promote to platform admin on the next boot). `auth.Repository.EnsureSeededAdmin` is now the *only* path to `is_platform_admin=true` — it creates one account named by `NIMBUS_ADMIN_EMAIL`/`NIMBUS_ADMIN_PASSWORD` if it doesn't exist yet, or leaves an existing account's password untouched and just ensures the flag (`ON CONFLICT (email) DO UPDATE SET is_platform_admin = true` — still promote-only/idempotent, matching the old semantics, just scoped to exactly one account instead of an arbitrary list). Both env vars are required (`config.validate()` fails fast without them); `docker-compose.yml`'s hardcoded `tier1-demo@nimbus.dev,test@gmail.com,test1@gmail.com` list is gone, replaced by `deploy/.env`-sourced credentials (added to `.env.example`, and to the Helm chart's `values.yaml`/`secret.yaml` for the k8s path too). One-time manual cleanup on the dev DB: the three previously-promoted demo accounts had their `is_platform_admin` flag explicitly revoked (nothing auto-revokes — same promote-only convention as before), so `admin@nimbus.dev` is now genuinely the only admin. Verified live: seeded admin logs in, `/v1/auth/me` shows `is_platform_admin: true`, and — after creating an org via the pre-existing org-less-user fallback screen (platform-admin status doesn't imply org membership) — the Cluster nav item appears and `/admin` renders real node/hash-ring data.
    - **Real security fixes applied**: (1) `ratelimit.ClientIP` took the *first* `X-Forwarded-For` hop, which is attacker-controlled — trivially defeats per-IP rate limiting the moment Caddy goes in front per the deferred VPS plan (a client could send a fresh fake XFF value every request); now takes the *last* hop, matching a single trusted reverse-proxy topology. (2) The root `.dockerignore` had no `.env*` exclusion, so `Dockerfile.web`'s `COPY frontend/ .` build-stage step could bake `frontend/.env.local` (which contained a live Vercel OIDC token) into a local Docker layer — added `**/.env`/`**/.env.*` (with a `!**/.env.example` carve-out) to `.dockerignore`. One finding deliberately **not** fixed here, left as an open decision at the time: global chunk dedup has no proof-of-possession check (a user who learns another org's chunk hash can reference it via dedup without owning the bytes — a real tradeoff of content-addressable storage, not a bug, worth a conscious call later — ~~closed in item 31~~, see below). The broader infra-exposure set from the deployment audit (Postgres/Redis/MinIO/Grafana/Prometheus all bound to `0.0.0.0` with weak shared dev credentials, no `restart:` policies, no resource limits) — those are VPS-provisioning decisions that belong with the already-deferred go-public work, not this session.
    - **Minor correctness/UX fixes from the audit's third angle** (all in the signup flow added in item 24): server-side 200-char cap on `org_name` (`auth.handler.go`) plus a matching client-side `maxLength` on the register form's input; the register-then-login two-call flow on `/register` now tracks whether the account was actually created before a subsequent login failure, so a transient login error after a successful signup shows "Account created, but sign-in failed… try signing in instead" rather than a generic error that looks like registration itself failed (the email is already taken at that point, so a blind retry would just 409).

26. (Full-codebase audit + accessibility session, 2026-08-08 — user asked for an extreme-detail documentation/rating pass over every aspect of the app, then to fix the audit's top accessibility finding) The audit itself was produced as a standalone rated report (19 aspects scored 0–10 with concrete file-level evidence, not checked into `docs/` — the user has the link), not a doc in this repo; this entry records the two follow-up work sessions it led to.
    - **Accessibility fix (roadmap item #4 from the audit: keyboard-operable custom controls + modal focus trapping)**: new `frontend/lib/useModal.ts` gives every overlay dialog the standard WAI-ARIA modal contract in one shared hook — auto-focus into the panel on open, Tab/Shift+Tab trapped inside it (with wraparound), Escape closes it, focus returns to the trigger element on close. Previously `ConfirmDialog`/`ShareDialog`/`MoveDialog`/`FileRow`'s `PreviewModal` each duplicated only the Escape-key half of this, with no trap and no `role="dialog"`/`aria-modal`. All four now carry `role="dialog"`, `aria-modal="true"`, and an accessible name (`aria-labelledby` pointing at the existing title, or `aria-label` where there's no heading element). Also fixed two genuinely keyboard-dead controls the audit flagged: `Thumb`'s preview trigger (`FileRow.tsx`) was a `role="button"` span with no `tabIndex`/`onKeyDown` at all — fixing it surfaced a real HTML constraint (it renders inside the row's own `<button>`, and a `<button>` can't contain focusable descendants, the exact reason `Checkbox` was already kept as a sibling rather than a child), so the icon/thumbnail slot was restructured to sit beside the row button instead of inside it, then given `tabIndex`/`onKeyDown`; and `UploadDropzone`'s drop-zone `<div>` — its hidden `<input type="file">` meant a keyboard user could not open the file picker through it by *any* means, not just an inconvenient one. `Tr` (the shared table-row primitive, `components/ui/Table.tsx`) got the same defensive fix even though no page currently passes it an `onClick`. Verified two ways: `tsc --noEmit`/`npm run lint` both clean, then a live pass against a real backend — registered a fresh account, drove the actual chunked-upload API directly from the browser console (bypassing the UI, since no file-upload automation tool was available) to get a real file and a real worker-generated thumbnail into the org, then confirmed on all four dialogs: auto-focus on open, Tab-trap forward/backward with wraparound, and Escape-close-returns-focus-to-trigger; separately confirmed `Thumb`'s icon now sits as a true DOM sibling of the row button (not nested) and that `UploadDropzone` is reachable via real Tab traversal. One caveat found and traced, not a defect: the browser-automation tool's own synthetic key-press action didn't trigger React's synthetic `onKeyDown` for the two JSX-prop-based handlers (`Thumb`, `UploadDropzone`) even though a manually-dispatched real `KeyboardEvent` did — `useModal`'s Escape/Tab handling uses a native `window.addEventListener` instead of a React prop and was unaffected. No automated test coverage was added for any of this — frontend test coverage remains zero (see "Known issues" below) — so a future regression in focus-trap behavior wouldn't be caught by CI.
    - **`kind` cluster found stale, rebuilt, and redeployed**: the live pass above surfaced that the long-running `nimbus`-named `kind` cluster (helm release revision 2, deployed 2026-07-04) was running images roughly 34 days old — predating several backlog sessions. `GET /v1/auth/me`, `GET /v1/folders/{id}/path`, and `POST /v1/orgs/{orgId}/shares` all 404'd (route genuinely absent, not an auth rejection), and `has_thumbnail` was missing entirely from folder-children responses rather than `false`. Note the previous handoff doc's claim that "the Day 13 kind cluster node container remains stopped, not deleted" was wrong by the time of this session — it had been running the whole time (see docs/next-session.md, corrected same session). Rebuilt all four images (`nimbus-api`/`nimbus-worker`/`nimbus-web`/`nimbus-migrate`) from current source — which also picked up this session's accessibility fixes and two other already-pending uncommitted changes (mandatory share-link expiry in `sharing/handler.go`, and the `NIMBUS_ORG_QUOTA_BYTES` Helm configmap addition matching `deploy/.env.example`) — `kind load docker-image`'d them into the `nimbus` cluster, `helm upgrade`'d (now revision 3; re-ran the migrate pre-install/pre-upgrade hook Job cleanly), and `kubectl rollout restart`'d `nimbus-api`/`nimbus-worker`/`nimbus-web` (`pullPolicy: Never` plus an unchanged image tag string means nothing would have picked up the freshly-loaded image content otherwise). Reverified live: the three previously-404ing routes now correctly 401 (unauthenticated) instead of 404 (route missing); `has_thumbnail` now correctly reports `true`/`false`; a bundle share link that 404'd before this redeploy now creates successfully (201); Postgres data survived untouched (same PVC); and the containerized `nimbus-web` pod itself (not just a local `next dev` server) serves the fixed frontend, confirmed by opening `PreviewModal` live through it. **No CI/automated check exists to catch this kind of drift recurring** — see the existing "Helm chart isn't in CI" gap below, which this incident is a concrete instance of.

27. (Doc-rendering fix, 2026-08-08, same day, after item 26 — user spotted a broken GitHub render of the README's architecture diagram) GitHub's Mermaid renderer failed to parse `README.md`'s architecture flowchart with a parse error at `subgraph API[nimbus-api  (modular monolith)]` — unquoted parentheses inside a `[...]` bracket label are ambiguous with Mermaid's other node-shape delimiters (the adjacent `Storage["Storage nodes (standalone MinIO x3+)"]` subgraph a few lines down already worked precisely because it was quoted). Fixed by quoting both offending labels (`API["nimbus-api (modular monolith)"]` and `WORKER["nimbus-worker\n(extracted service)"]`) in `README.md` — and, on checking further, found and fixed the identical bug in **`docs/02-system-design.md`**, which turned out to be the actual original source the README diagram was copied from (same two lines, never caught before because nobody had looked at that doc's rendered form on GitHub, only the README's). A follow-up systematic sweep of all 16 markdown files in the repo (fence balance, all 4 Mermaid diagrams individually — not just the 1 originally reported, table column/separator validity, all 29 relative markdown links + the 1 anchor link, stray unescaped HTML) found nothing else broken. One near-miss noted for future reference, not a bug: `docs/07-distributed-architecture.md`'s Redis key-schema table uses a backslash-escaped pipe (`` `healthy`\|`down` ``) inside a cell — correct GFM syntax (renders as a literal `|`, doesn't split the column), but looks like a bug to a naive raw-pipe-count check.

28. (Quick-wins session, 2026-08-08, same day as items 26–27 — user picked the audit's "quick wins" bucket to work through instead of the go-public VPS plan) Five of the audit's roadmap items (docs/00's linked audit report §19), all built and live-verified:
    - **Pinned the 5 floating `:latest` image tags** (roadmap #2) — `minio/minio` (all 3 Compose services + all 3 k8s StatefulSets), `axllent/mailpit`, `prom/prometheus`, `grafana/grafana` now pin exact verified-current versions (`RELEASE.2025-09-07T16-13-09Z`, `v1.30.6`, `v3.13.2`, `13.1.3` respectively) in both `deploy/docker-compose.yml` and `deploy/k8s/infra/*.yaml`. Nimbus's own `nimbus-{api,worker,web,migrate}:latest` in the Helm chart's `values.yaml` deliberately stays as-is — different pattern (`pullPolicy: Never`, loaded straight from a local build via `kind load docker-image`, not a registry pull).
    - **`govulncheck` + `npm audit --audit-level=high` added to CI** (roadmap #3) — and both scanners' real findings were fixed rather than just wired in red: backend had `golang.org/x/text@v0.38.0` (bumped to `v0.39.0`) and a `crypto/tls` stdlib ECH privacy leak fixed only in `go1.26.5` (backend/go.mod now pins `toolchain go1.26.5`, which `go build`/CI auto-downloads); frontend had 5 high-severity advisories against `next@16.2.10`'s range (bumped to `16.3.0`, still within the 16.x line — `npm run lint`/`build` both clean after) plus a transitive `js-yaml` issue `npm audit fix` closed on its own. Both scanners report zero findings now.
    - **Non-root `USER` in all four Dockerfiles** (roadmap #7) — `Dockerfile.api`/`Dockerfile.worker` add an `adduser`'d `nimbus` (uid 10001) on the from-scratch `alpine:3.20` runtime stage; `Dockerfile.web` runs as `node:22-alpine`'s existing `node` user (`--chown=node:node` on the standalone-output `COPY`s); `Dockerfile.migrate` adds a `migrate` user on top of the third-party `migrate/migrate:v4.17.1` base (confirmed Alpine-based with `adduser` available). All four verified: built clean, `id` shows the non-root uid inside each running container, and `nimbus-web` still actually serves (curl 200) as `node`.
    - **Dedicated, tighter rate-limit bucket for `/v1/auth/login` and `/v1/auth/login/totp`** (roadmap #6) — these two routes run before any access token exists, so the general per-user/per-IP limiter's bucket never covers them; a second `ratelimit.Limiter` now wraps just those two, keyed by IP alone (`"login-ip:" + ClientIP`, never a user-ID key), defaulting to 1 rps / burst 5 (`NIMBUS_LOGIN_RATE_LIMIT_RPS`/`_BURST`, 0 disables). Verified against a real disposable Redis container (not mocked): 5 requests allowed, 6th denied within the same second, a different source IP got its own independent bucket — then the throwaway test file was deleted, this isn't permanent coverage. Also fixed real doc drift found while touching this area: `docs/04-lld.md` §4 still claimed `ratelimit.ClientIP` reads the *first* `X-Forwarded-For` hop, three sessions after item 25 actually changed it to the *last* hop — corrected.
    - **Grafana alert on the DLQ** (roadmap #9) — `dead_events` had no signal beyond someone remembering to check the admin panel. `internal/events.Repository.RegisterMetrics` adds `nimbus_dlq_dead_events`, a `GaugeFunc` read live from Postgres (`SELECT count(*) WHERE status='dead'`) at Prometheus scrape time — deliberately *not* an in-process counter, because inserts happen in nimbus-worker and resolves (retries) happen via nimbus-api's admin endpoint, so a per-process gauge (like `StorageNodeHealthy`) would go negative the moment a retry lands on a process that never saw the matching insert. A new Grafana alert rule + contact point + notification policy (`deploy/observability/grafana/provisioning/alerting/`, Compose only) fires when the gauge is `> 0` for 5 minutes, routed to a `nimbus-ops` email contact point through the same Mailpit catcher password-reset email already uses (`GF_SMTP_*` added to the `grafana` Compose service) — no real SMTP relay needed to see it work. **Verified genuinely end-to-end on a live Compose stack**: inserted a real row into `dead_events`, watched `nimbus_dlq_dead_events` go from 0→1 in Prometheus, watched the rule transition Pending→Alerting (`for:` temporarily dropped to 10s for the test, reverted to 5m after), and fished the resulting `[FIRING:1] DLQ has unresolved dead events` email out of Mailpit's API. Compose-only by the same logic as the existing "Mailpit is Compose-only" gap below — kind's Grafana has no alerting provisioning mounted (`deploy/k8s/infra/apply.sh` only ConfigMaps the datasource/dashboard files) and no Mailpit to deliver to either, so wiring it there wouldn't actually notify anyone.
    - **Verification note**: this session needed the Compose stack up while the long-running `nimbus` kind cluster was also up, which don't coexist (shared host ports) — the kind control-plane container was `docker stop`'d (not deleted) for the duration and restarted after. One unrelated port conflict surfaced along the way: an unrelated project's `pulseboard-redis` container already held host port 6379; it was stopped for the verification window and restarted immediately after. A `nimbus-redis-1` container also came up with an empty `NetworkSettings.Networks` (not attached to `nimbus_default`) on the first `up` — force-recreating just that one service fixed it; noted here in case it recurs, cause not fully diagnosed (plausible: the network wasn't fully ready yet when compose tried to attach it, racing the earlier port-conflict-aborted `up` attempt).
    - **Not done in this session** (deliberately deferred, no user ask yet): roadmap #1 (httpOnly refresh-token cookie via a Next.js BFF proxy — the audit's single highest-leverage security fix, but Medium effort touching the auth flow — ~~built in item 31~~) and the Testing/UI-UX/Security-hardening buckets the user didn't pick this round.

29. (Architecture-gap session, 2026-08-08, same day as item 28 — user asked to fix all three gaps the audit's §01 "Architecture & module boundaries" section named) All three fixed and verified:
    - **`cmd/api/main.go` split from 518 lines into six files** — `main.go` (170 lines: config/infra setup, then sequences the wiring calls below, then final middleware assembly/server start), `adapters.go` (the 10 cross-module port-adapter types, unchanged, just relocated), and one `wire_*.go` per concern following the audit's own suggested grouping (`wire_auth.go`: auth+org, mutually dependent at construction time; `wire_storage.go`: the storage router plus the `/v1/admin/*` cluster-ops routes it backs; `wire_files.go`: folder+file+search; `wire_activity.go`: activity+SSE; `wire_upload.go`: upload+sharing, both built last since both depend on file/folder/activity). Verified two ways: a route-string diff between the pre-refactor `main.go` and the six new files found the exact same 56 unique routes registered, byte-for-byte; then a live end-to-end smoke test against a rebuilt Compose stack (register→login→org→folder→dedup-check→chunked-upload-with-real-presigned-PUTs→commit→complete) passed clean.
    - **`internal/admin` discoverability** — the audit's own read was that the project's "permanent, not a gap" call is fair, so this wasn't restructured; instead `internal/events` and `internal/storage`'s package doc comments (`types.go` in each) now say outright that they're where the `/v1/admin/*` cluster-ops routes actually live and point at this doc's "Known issues" entry for why. `docs/08-folder-structure.md`'s matching bullet was also corrected — it had drifted to still describe `internal/admin` as "a reserved, currently-empty package" (implying it was still planned), which is stale now that this doc calls the split permanent.
    - **`ChunkSizeBytes`/`WriteQuorum` config fields were dead weight — now both are real, enforced checks in `upload.Service.CommitChunk`**, not just loaded/validated/logged. `WriteQuorum` (default 2, matching `docs/02-system-design.md`'s already-documented "W=2 of N=2" design) now rejects a commit with `ErrInsufficientReplicas` (409) if fewer than `writeQuorum` replicas' etags are present — previously *any* non-empty, mutually-agreeing etag set was accepted regardless of count, an implicit and undocumented W=1. `ChunkSizeBytes` (default 8 MiB) now rejects a commit with `ErrChunkTooLarge` (413) if the declared chunk size exceeds it. Both verified live against a real Compose stack: a 1-etag commit (quorum=2) correctly 409'd, a ~9.5 MB declared chunk (cap 8 MiB) correctly 413'd, and the normal 2-replica, ≤8 MiB happy path still committed and completed cleanly. Caveat recorded in `CommitChunk`'s doc comment: the frontend (`lib/upload.ts`) still aborts the whole chunk on any single PUT failure rather than tolerating down-to-`writeQuorum` partial success, so the "accept fewer than N, as long as ≥ W" path this enables is real and reachable but not yet exercised by today's client — a separate, larger frontend-resilience change, out of scope here.
    - All three fixes verified with `go build`/`go vet`/`gofmt` clean, `go test ./... -short` green, and the live Compose smoke tests above; no unit test coverage was added for `CommitChunk`'s new checks specifically (the `upload` package still has zero unit test files, a pre-existing gap — see item 26/the audit's §14).

30. (Second architecture-gap session, 2026-08-08, same day as items 28–29 — user asked to fix all three gaps the audit's §02 "Distributed storage engine" section named) All three fixed and live-verified against a rebuilt Compose stack:
    - **A real repair/re-replication pass, matching what docs/02-system-design.md had described since its original write-up but never actually existed** — `POST /v1/admin/nodes/{nodeId}/repair` (`internal/storage/repair.go`, platform-admin gated like the other `/v1/admin/*` routes) re-verifies every chunk Postgres records as committed on a node against what's physically present (`StatObject`), and re-copies from a surviving replica (`GetObject`→`PutObject`, both already existed for the worker's thumbnail path) whatever's missing — the standalone-MinIO durability gap the audit named ("losing a MinIO container's volume loses that node's copies outright, with no compaction/repair job to re-replicate afterward"). Manual-trigger by design, not automatic-on-recovery, to avoid designing repair-storm/backoff protection for a failure mode (real volume loss) a health-flap doesn't actually cause — see the file's own doc comment. Also gave real meaning to `chunk_locations.status`'s `'degraded'` enum value, defined in migration 000001 but never once set by any code path until now: a chunk found missing is marked degraded before the repair copy is attempted, and flipped back to committed only on success — a chunk with no surviving replica anywhere stays honestly degraded rather than silently committed. **Verified live, three scenarios**: (1) deleted one replica's object directly from a MinIO container's filesystem (simulating volume loss) while `chunk_locations` still said committed, ran repair, confirmed the response (`{"checked":2317,"restored":1,"unrepairable":0}`), the object physically back on disk, and the row still correctly `committed`; (2) deleted the *same* chunk from *both* its replicas (total loss) and reran repair, confirmed `unrepairable:1` and the row correctly flipped to `degraded`; (3) the normal case (2317 other already-intact chunks from this session's earlier testing) all correctly reported as checked-and-untouched.
    - **Latency-aware placement** — `Router.Resolve` is now two-tier: a first pass considers only healthy nodes whose rolling probe-latency EWMA (new, alpha=0.3, seeded from the first successful probe) is under `NIMBUS_STORAGE_SLOW_THRESHOLD` (default 200ms); only if that pass can't fill the replication factor does a second pass fall back to every healthy node regardless of latency. A technically-alive-but-slow node now sinks behind faster ones instead of being treated identically (the audit's exact phrasing). New `nimbus_storage_node_latency_ms` gauge for observability, confirmed live (~1-1.6ms on all 6 process×node series against local Docker MinIO, comfortably under the default threshold — expected on a healthy local stack). The two-tier *logic* itself was verified with three new real unit tests in `internal/storage/router_test.go` (`TestResolve_SinksSlowNodeBehindFasterOnes`, `TestResolve_SlowNodeStillFillsGenuineShortfall`, `TestResolve_ZeroThresholdDisablesTiering`) rather than live network-latency injection, which isn't practically reproducible in this environment — the tests construct a real `Router` via the production `NewRouter` path (against never-dialed fake endpoints — MinIO client construction is lazy) and set the unexported health/latency maps directly, same-package.
    - **A Grafana alert on cross-process health disagreement** (`nimbus-storage-health-disagreement`, same `deploy/observability/grafana/provisioning/alerting/rules.yml` the DLQ alert lives in) — fires when `nimbus-api` and `nimbus-worker`'s independent `nimbus_storage_node_healthy` series for the same node genuinely disagree (`max by (node) (...) - min by (node) (...) > 0`) for 2+ minutes, routed through the same Mailpit contact point as the DLQ alert. The two-gauge design itself is unchanged and still deliberate (see "Important design decisions" below) — this only closes "nothing surfaces that disagreement to an operator." Verified live: both rules provisioned with no errors, and the disagreement query correctly returned `0` for all three nodes against a healthy, agreeing stack (the no-false-positive case). Genuinely forcing the two processes to disagree for 2+ minutes would need real network-partition tooling (e.g. `iptables`/`tc netem` isolating one container's route to MinIO from just one of the two Go processes) not readily available in this Windows/Docker Desktop environment — noted here rather than silently skipped; the query's correctness in the "agree" case plus the trivial arithmetic of `max-min` over a two-value {0,1} set is what the "disagree ⇒ 1" claim rests on.
    - All three verified with `go build`/`go vet`/`gofmt` clean and `go test ./... -short` green (including the three new router tests) in addition to the live Compose checks above.

31. (§03/§04/§05 audit-fix session, 2026-08-08, same day as items 26–30 — user asked to fix every gap named in the audit's §03 "Upload pipeline", §04 "Authentication & session security", and §05 "Authorization & multi-tenancy" sections, all of them) Eight gaps closed across three sections, every one live-verified against a rebuilt Compose stack plus the full smoke suite:
    - **§03 — streaming chunk reads, not one `arrayBuffer()` call.** `frontend/lib/upload.ts` now reads the file one `File.slice()` chunk at a time (peak memory: one ~8 MiB chunk, not the whole file) instead of `file.arrayBuffer()`ing it all up front. Per-chunk content hashes still use `SubtleCrypto.digest` (each slice is already bounded); the whole-file checksum needed by `/complete` uses a new hand-rolled streaming SHA-256 (`frontend/lib/sha256-stream.ts`, ~110 lines, FIPS 180-4) since browsers have no incremental `SubtleCrypto` digest API — verified correct against Node's `crypto` module across empty input, exact-block-boundary, and multi-megabyte random inputs before it was ever wired into the upload path.
    - **§03 — resume after a page reload.** A page refresh loses `uploadFile`'s closed-over JS state, but content-addressed dedup already means a fresh `init` + `checkChunks` pass skips re-uploading anything an earlier, interrupted attempt managed to commit — the only real loss was a second orphaned `uploads` row. `upload.ts` now persists `{upload_id, totalBytes, savedAt}` to `localStorage` keyed by a `name|size|lastModified|target` fingerprint (7-day TTL) and reuses the same `upload_id` on retry instead of calling `init` again; cleared on success, or on a 404/409 (dead upload) so a genuinely-gone session doesn't retry forever. No backend change needed — the server-side state this leans on already existed.
    - **§03 — client-side download integrity check.** `FileRow.download()` and the share page's `assembleFromPlan` each used to accept whatever the *first* successful HTTP fetch returned, with only network failures triggering the next replica. Both now share one implementation (`frontend/lib/download.ts`, new — the two call sites were near-identical, so this also removed a real duplication) that re-hashes every fetched chunk against the content hash the download plan already carries (`DownloadPlanChunk.hash`, already returned by the API) and falls back through the remaining `targets` on mismatch, not just on outright fetch failure — matching the integrity guarantee the worker's own reassembly path already had.
    - **§04 — refresh token off `localStorage`, onto an `httpOnly` cookie (roadmap #1, the audit's single highest-leverage fix).** New `app/api/auth/{login,login/totp,refresh,logout}` route handlers are a thin Next.js BFF proxy in front of the Go API: they're the only place the refresh token is ever JS-reachable, setting/reading it as an `httpOnly`, `SameSite=Strict` cookie and handing the client only the short-lived access token. `frontend/lib/tokens.ts` now holds the access token in memory only (module-level variable, not `localStorage`); `AuthProvider` (`lib/auth-context.tsx`) determines "was I logged in?" on a cold load by calling the refresh route rather than checking `localStorage` for a refresh token. Needed a server-side-only `NIMBUS_API_INTERNAL_URL` (`frontend/lib/server/auth-proxy.ts`) distinct from the browser-facing `NEXT_PUBLIC_API_URL`, since the containerized `nimbus-web`'s route handlers run inside a container where `localhost` doesn't resolve to `nimbus-api` — set to `http://nimbus-api:8080` in both `docker-compose.yml` and the Helm chart's `web-deployment.yaml`, defaulting to `NEXT_PUBLIC_API_URL` for host `npm run dev`. Live-verified two ways: `curl` against the real route handlers confirmed the `Set-Cookie` is `httpOnly`/`SameSite=strict` with no `refresh_token` in the JSON body, refresh rotation works, reusing an already-rotated cookie 401s *and* revokes the whole family (so the rotated-but-unused cookie 401s too), and logout clears the cookie (`Expires=Thu, 01 Jan 1970`); then a real browser session confirmed login → F5 hard reload → still authenticated with real data loading. **A real concurrency bug was caught by this live testing, not by review**: `AuthProvider`'s bootstrap call and `request()`'s 401-retry path both call `refresh()`, but only the latter had the in-flight-promise dedup guard — React 18 Strict Mode double-invoking the mount effect in dev fired two concurrent refresh calls on *every single page load*, and the loser's reuse of an already-rotated token correctly-but-unintentionally revoked the user's own session. Fixed by moving the guard inside `refresh()` itself so every caller shares one in-flight promise; confirmed fixed by the same F5 test only logging one `/api/auth/refresh` call afterward, not two.
    - **§04 — JWT secret rotation support.** `NIMBUS_JWT_SECRET_PREVIOUS` (optional, `config.go`) lets an access token signed under a just-rotated-out secret keep verifying until it naturally expires (≤15 min) instead of every live session breaking the instant the secret changes — `auth.tokenIssuer.verify` tries the current secret first, falls back to the previous one only on failure; new tokens are always issued under the current secret. Empty disables the fallback entirely (unchanged default behavior).
    - **§04 — `NIMBUS_ADMIN_PASSWORD` complexity requirement.** Was length-only (≥8, same bar as regular signup passwords) despite gating cluster-wide `/v1/admin/*` access. `config.validateAdminPassword` now requires ≥12 characters and at least 3 of uppercase/lowercase/digit/symbol — a real fail-fast check, not just a stronger comment. `deploy/.env.example` and the Helm chart's `values.yaml` placeholder passwords were both upgraded to satisfy it (the real `deploy/.env` password already did, confirmed by the rebuilt `nimbus-api` booting clean).
    - **§05 — short-lived cache for the membership/role check.** `org.RequireRole`'s `repo.GetMembership` call fronted by a small in-process, 5-second TTL cache (`org/membership_cache.go`, new) shared across every route a given `RequireRole` instance gates (the `requireMember`/`requireOrgAdmin` middleware built once at startup) — no explicit invalidation on `AddMember`/`RemoveMember`, the short TTL alone bounds staleness, matching the audit's own framing of this as a minor latency optimization rather than a correctness issue. In-process rather than Redis-backed deliberately, to avoid a new cross-replica invalidation path for a gap this size.
    - **§05 — dedup proof-of-possession, the meatiest fix.** Confirmed exploitable before this session by direct API reproduction: an org that only ever learned another org's chunk hash (never the bytes) could put that hash in `chunk_order` at `/complete` and succeed, because `FindMissingChunks` checked pure global existence with no ownership concept. Fixed with a new `org_chunk_proofs (org_id, chunk_hash)` table (migration `000014`) recording which chunks *this* org has actually proven via a real `CommitChunk` (etags already verified against real replica PUTs) — `upload.Repository.FindMissingChunksForOrg` now requires either a commit under the current upload session or a prior proof row before a hash counts as present, for both the dedup-check endpoint and `/complete`'s validation. Physical storage dedup is untouched (`chunks`/`UpsertGlobalChunk` stay global — identical bytes are still never stored twice); what changed is that *skipping the upload* now requires this org to have earned it once. This also moved the dedup-check endpoint from a bare global `POST /v1/chunks/check` to `POST /v1/uploads/{uploadId}/chunks/check` (org resolved via the upload session, same `RequireAccess` middleware `InitChunk`/`CommitChunk`/`Complete` already use) — `frontend/lib/upload.ts` and all seven `scripts/smoke-*.js`/`load-upload.js` scripts that called the old route were updated to call `init` before `checkChunks` instead of after. **Directly reproduced the attack end-to-end pre-fix and confirmed it's closed**: a live two-org test (org A genuinely uploads and commits real bytes; org B, a fresh account with no access to those bytes, calls `checkChunks` and then `/complete` using only the leaked hash) now gets `checkChunks` correctly reporting the hash as still-missing and `/complete` correctly 400ing with "one or more chunks in chunk_order were never committed" — where before this session's fix both would have silently succeeded. Legitimate same-org dedup (re-uploading the same file, or a second file sharing content within one org) still works — confirmed via `scripts/smoke-upload.js`'s existing "second upload of identical content, must dedup all chunks" check, still passing (0 missing, 3/3 deduped).
    - **Verification**: `go build`/`go vet`/`gofmt` clean, `go test ./... -short` and `go test -tags=integration ./...` both green (a live Postgres/Redis was available this session, so the integration suite actually ran, not just compiled); frontend `tsc --noEmit`/`npm run lint`/`npm run build` all clean. Live Compose smoke suite (`smoke-upload`, `smoke-versions`, `smoke-sharing`, `smoke-search-activity`, `smoke-thumbnails`) all green after being updated for the `chunks/check` route move; `smoke-gc.js` was updated for the same route move but not run to completion (its full mark/sweep cycle needs a hand-tuned short `NIMBUS_GC_INTERVAL`/`NIMBUS_GC_GRACE` on the worker container, which wasn't configured this session — the specific SQL path it would exercise was already covered by the passing dedup tests above). Two environment issues hit and fixed along the way, unrelated to the code changes: the recurring `nimbus-redis-1` empty-network-attachment Docker Desktop glitch from item 28 recurred (fixed the same way, force-recreate), and `deploy/.env`'s `NIMBUS_STORAGE_NODES` still pointed at dead Cloudflare Quick Tunnel hostnames from the 2026-07-06/07 self-host experiment (commented out, falls back to the Compose default — noted in the file for whoever relaunches the tunnels next).

32. (Roadmap #8 + #13 session, 2026-08-08, same day as items 26–31 — user picked the audit's remaining roadmap items to work through one at a time) Two items closed:
    - **Roadmap #8 — toast-with-undo for trash/delete.** New `frontend/components/Toast.tsx` (`ToastProvider`/`useToast`, same context pattern as `lib/auth-context.tsx`) mounted in the root layout. Trashing a file (`FileRow.trash`) or folder (`FolderPage.trashFolder`) now shows a dismissable toast with an **Undo** button that calls the same `restore` endpoint the Trash page already used — cheap given that endpoint already existed, per the audit's own framing. Deliberately not applied to the Trash page's "Delete forever" (purge) — that's genuinely permanent with no restore endpoint, already correctly guarded by `ConfirmDialog`'s explicit warning, and undo affordance there would be misleading. Toast uses `role="status"`/`aria-live="polite"` (an always-mounted live region, not conditionally rendered) matching the accessibility bar the rest of the app is held to since item 26. **Live-verified**: registered a test account, created and trashed a folder, confirmed the toast and its message, clicked Undo, confirmed the folder reappeared — network tab showed the real `DELETE`/`POST .../restore` calls succeeding (204/200).
    - **Roadmap #13 — `helm lint`/`helm template`/kind smoke-deploy in CI.** New `helm-smoke-deploy` job in `.github/workflows/ci.yml`: `helm lint` and `helm template` run first as a fast fail, then a real `kind` cluster is created (`helm/kind-action@v1`, pinned to the same `kind`/`kubectl` versions verified locally), the four images are built and `kind load`ed, `deploy/k8s/infra/apply.sh` applies the stateful infra (unmodified — its Windows/Linux path-resolution fallback already degrades correctly on a Linux runner), `helm install --wait` deploys the chart, and the job curls `/healthz`/`/readyz`/`/api/health` through the same NodePort URLs the documented local recipe uses. A failure dumps pod state/logs before failing. Closes the audit's "Helm chart isn't in CI" gap (§15/§19 row 13) — the exact gap that let a kind cluster silently run ~34-day-stale images (item 26) with nothing catching it. **Verified as far as this environment allows without disrupting the already-running local `nimbus` kind cluster**: `helm lint`/`helm template` both run for real against the actual chart (0 errors); the rendered manifests were also validated with `kubectl apply --dry-run=server` against the real cluster's API (schema-valid, no rejected fields); the workflow YAML parses and stages jobs correctly (`act -l`). The kind-cluster-creation-through-curl portion of the new job was **not** run end-to-end locally — doing so would mean either standing up a second competing kind cluster or tearing down the one already in use for other verification, and `act`'s job containers don't reliably get the Docker-in-Docker access `kind` needs (a known `act` limitation, not specific to this workflow). The steps themselves aren't new/invented, though: they're the same sequence (`kind create` → build+load images → `apply.sh` → `helm install`) already run manually and working per item 26's own recovery, just moved into CI.

33. (Roadmap #11 session, 2026-08-08, same day as items 26–32 — user asked to generate an OpenAPI spec from the routes and derive the frontend types from it, the audit's highest-remaining-effort item) Closes §07/§10's "checked by hand"/"hand-maintained in parallel" complaints with a real generation pipeline, not just a relabeled hand-written doc:
    - **`backend/internal/apidoc`** (new) — ~40 exported Go DTO structs mirroring every response/request shape the audit's own field-by-field source read confirmed handlers actually produce (every `httpserver.WriteJSON(w, ..., map[string]any{...})` call site across all 9 domain modules was individually checked against these structs and against the pre-existing `frontend/lib/types.ts`/`lib/api.ts`, which is what the structs were transcribed from). Deliberately *not* imported by any handler — handlers keep writing `map[string]any` — so this stays a pure documentation-generation input, not a rewrite of already-audited, working handler code. That's a real, disclosed limitation: a handler that changes its response shape without a matching edit here won't be caught by the compiler (see the package's own doc comment).
    - **`backend/cmd/openapi-gen`** (new, ~500 lines, zero new dependencies) — parses `cmd/api/*.go`'s actual `mux.Handle`/`mux.HandleFunc` source (via `go/ast`, not execution) to extract every one of the 57 real routes' method/path/auth-middleware-wrapper, so *that* part can never drift from what's actually registered — the generator hard-fails the build if a registered route has no entry in its per-route schema table, so missing coverage is a loud build error, not a silent gap. Reflects `internal/apidoc`'s structs into OpenAPI 3.0.3 JSON Schema (nested structs, maps, slices, pointer-as-nullable, `time.Time`→`date-time`, nil-interface→unconstrained-not-`Record<string,never>` after a bug caught and fixed mid-session) via a hand-rolled ~100-line schema builder — a dependency the size of `kin-openapi` wasn't worth it at this route count, matching this backend's existing no-framework bar. Emits `docs/openapi.json` (checked in, 3,947 lines).
    - **`frontend/lib/api-schema.generated.ts`** (new, checked in) — generated from `docs/openapi.json` via `openapi-typescript` (new devDependency; pulled in a transitive `js-yaml` CVE via `@redocly/openapi-core`, closed with an `overrides` pin in `package.json`, confirmed via `npm audit --audit-level=high` — 0 vulnerabilities). `frontend/lib/types.ts` was rewritten from a ~170-line hand-maintained file into a ~55-line thin re-export layer over the generated schema — every previously-exported name (`FolderNode`, `FileSummary`, `OrgUsage`, ...) still exports from the same module, so no other frontend file needed to change (confirmed: `tsc --noEmit` clean, `npm run build` clean, only two files besides `types.ts` itself touched — `package.json`/`package-lock.json`). Three TypeScript string-literal unions that don't survive the Go→OpenAPI round trip (Go has no enum type) were deliberately kept hand-declared rather than derived: `OrgRole`, `DeadEvent.status`/`StorageNode.status`, and the `ResolvedShare` discriminated union's `kind` tag — each is a small `Omit<generated, "field"> & {field: "literal"}` override, documented in `types.ts`'s own header comment as intentionally not derived.
    - **CI enforcement, both directions, live-verified with real `act` runs (not dry-runs) against the actual runner image**: `build-test` gained an "openapi spec is up to date" step (regenerates `docs/openapi.json` fresh and diffs against the checked-in copy) and `frontend` gained a matching "generated api types are up to date" step — together they close the staleness loop end-to-end: a route/schema change without regenerating either file now fails CI instead of silently drifting the way `docs/06-api-design.md` used to. **Both steps, plus the full existing `build-test` and `frontend` jobs (vet/build/unit-test/govulncheck, and lint/build), were run for real via `act -P ubuntu-latest=catthehacker/ubuntu:act-latest`** (not just `-n` dry-run) against a freshly-cloned checkout in the actual runner container — genuinely different from item 32's `helm-smoke-deploy` job, which needs Docker-in-Docker `act` can't provide; these two jobs have no such dependency and both passed clean end-to-end, `npm run build` included.
    - `docs/06-api-design.md`'s status line now says explicitly that `docs/openapi.json` is the source of truth for the route table as of this session, not the hand-written tables below it (kept for prose/rationale a schema alone doesn't carry — auth flow ordering, error-code reasoning, idempotency/pagination notes).
    - **Not done, disclosed rather than silently skipped**: `frontend/lib/api.ts`'s own inline anonymous response types (e.g. `request<{ user_id: string; email: string }>(...)`) were left as-is — only the named exports in `types.ts` were in scope, to keep the diff to the one file the audit actually named as hand-maintained-in-parallel; no unit test coverage was added for `cmd/openapi-gen` itself (verified by its actual output matching hand-inspection plus the CI diff-checks passing for real, not by a test suite).

34. (Roadmap #14 session, 2026-08-08, same day as items 26–33 — user asked to wire OpenTelemetry spans across HTTP → NATS → worker with Grafana Tempo as the backend, the audit's last remaining roadmap item) Closes §09's "no distributed tracing" gap for real, not just a library import:
    - **`backend/internal/platform/tracing`** (new) — installs the OpenTelemetry SDK's global `TracerProvider`/W3C-tracecontext propagator, exporting via OTLP/HTTP. `NIMBUS_OTEL_EXPORTER_ENDPOINT` optional, same graceful-degradation shape as `SMTPAddr`/mailer: empty leaves the SDK's own genuine no-op provider in place rather than erroring, so every `tracing.Tracer(...).Start` call elsewhere is safe unconditionally.
    - **HTTP hop** — `httpserver.Tracing` (new middleware, placed to wrap `Recoverer` so a recovered panic's 500 still lands on the span before it ends) starts a server span per request named by the mux-matched route pattern, same cardinality reasoning `Metrics` already uses. Extracts an inbound `traceparent` first so a request already part of a trace joins it.
    - **NATS hop** — `events.Publisher.PublishUploadCompleted` now injects the calling request's trace context into the outgoing message's headers (switched `Publish` → `PublishMsg` to carry them); `events.Subscribe` extracts it back out and starts a `SpanKindConsumer` span before invoking the handler — a `headerCarrier` adapts `nats.Header` (identical shape to `http.Header`) to `propagation.TextMapCarrier`. This is the part that actually proves cross-process propagation, not just two processes independently exporting spans to the same backend.
    - **Worker processing** — `internal/processing.Processor.Process` wraps `reassemble`/thumbnail-generation/`PutObject` in three named child spans (`reassemble_chunks`, `generate_thumbnail`, `store_thumbnail`) via two small generic helpers (`traced`/`tracedValue`), giving the trace real internal structure instead of one opaque worker span.
    - **Grafana Tempo** added to both environments — `deploy/docker-compose.yml`'s new `tempo` service (`grafana/tempo:2.9.0`, pinned per the audit's own image-pinning convention, config at `deploy/observability/tempo.yaml`) and `deploy/k8s/infra/tempo.yaml` (ClusterIP-only — Grafana queries it in-cluster, nothing outside the cluster needs to reach it, so no NodePort). Grafana's datasource provisioning (`deploy/observability/grafana/provisioning/datasources/datasource.yml`) gained a Tempo entry wired to the same Prometheus datasource via `tracesToMetrics`, so a trace and its request's own histogram bucket are one click apart. `apply.sh` and the Helm chart's `configmap.yaml` (`NIMBUS_OTEL_EXPORTER_ENDPOINT` pointing at the in-cluster Service) were updated to match.
    - **Live-verified end-to-end against a real Compose stack, not just schema-checked** — the already-running `kind` cluster was briefly `docker stop`'d (reversible; shares host ports with Compose, same pattern noted in item 28) to free the ports, `tempo`/`postgres`/`redis`/`nats`/`minio`×3/`nimbus-api`/`nimbus-worker` were brought up, and `scripts/smoke-thumbnails.js` (unmodified, pre-existing) was run against it to exercise the real upload → thumbnail pipeline. Queried Tempo's own search/trace API afterward and confirmed the exact shape roadmap #14 asked for: one trace rooted at `nimbus-api`'s `POST /v1/uploads/{uploadId}/complete` span, with `nimbus-worker`'s `process nimbus.uploads.completed` span correctly parented to it (proving the NATS-header trace-context propagation actually worked, not just that both processes happened to export to the same backend), and `reassemble_chunks`/`generate_thumbnail`/`store_thumbnail` correctly nested one level deeper under that. The `kind` cluster was restarted afterward and confirmed back to all-pods-`Running` before ending the session.
    - **Not done, disclosed rather than silently skipped**: `events.Publisher.PublishRaw` (the DLQ retry republish path) does not inject trace context — a manual admin retry starts a fresh trace rather than continuing the original one, a deliberate scope cut (the audit named the upload-complete path specifically, not DLQ retry) rather than an oversight. No unit tests were added for the tracing middleware/carrier/helpers themselves — verified live via the real Tempo trace tree above instead.

37. (Testing & verification session, 2026-08-09, after item 36 — user said §14's 5.8/10 was too low and asked to fix everything/test everything) Closes the audit's roadmap #5 (the last open row of the original 17) and goes well beyond its literal scope — 7 of the 9 non-`auth`/`upload`/`storage` backend modules had zero automated tests before this session, and the frontend had none at all:
    - **Backend: real Go test files added for `org`, `sharing`, `folder`, `file`, `gc`, `search`, and `events`** (previously zero coverage; `auth`/`upload`/`storage` already had the project's original 4 test files) — a mix of white-box unit tests for pure logic (`org`'s `membershipCache`/role-elevation guard clauses, `search`'s cursor encode/decode, `events`'s `headerCarrier`) and `//go:build integration` tests against real Postgres (all seven modules) plus real MinIO and NATS for `gc` and `events` specifically, matching the house style `internal/auth/refresh_integration_test.go` already set. `gc`'s suite ports the correctness properties `scripts/smoke-gc.js`'s 13 assertions cover (dedup-survives-while-referenced, mark-dooms-after-grace, doomed-chunks-resurrect-on-recommit, sweep-physically-deletes-row-and-MinIO-object, trash-auto-purge-feeds-mark) with SQL-backdated timestamps instead of the smoke script's real multi-minute wall-clock waits; the down-node container-kill scenario (its step 8) is out of scope, staying a manual/smoke concern like the chaos/load scripts already were. `sharing`'s suite ports the audit's other named target — expiry validation, natural-expiry rejection, non-member-revoke-403, owner-revoke-then-404, cross-org bundle rejection. A new `upload` integration test covers quota enforcement end-to-end (`InitUpload` → `checkQuota` → real `file.Repository.OrgUsageBytes`), which the audit flagged as having no coverage anywhere, not even a smoke script.
    - **Real bugs caught by actually running the new tests, not just compiling them** — exactly the class of thing code review alone misses, per this project's own stated verification philosophy: (1) a chunk hash referenced in `file_version_chunks` without a corresponding `chunks` row first — a real FK constraint the tests initially violated with placeholder hashes, fixed by seeding real chunk rows via `upload.Repository.UpsertGlobalChunk` before referencing them; (2) `file_versions.checksum_sha256`/`chunks.hash` are fixed-length `char(64)` columns — Postgres space-pads short placeholder strings on read, so equality assertions against a short literal silently failed; fixed by using real 64-hex-char digests (`sha256("seed")`) throughout instead of arbitrary strings; (3) backdating `chunks.last_seen_at`/`doomed_at` by binding a Go `time.Duration` directly hit a type Postgres can't subtract from a `timestamptz` — pgx has no built-in `time.Duration`→`interval` mapping — fixed by switching to the same `now() - ($1 * interval '1 second')` pattern `gc.Sweeper`'s own production queries already use; (4) a DLQ-handler test matched dead-events by a fixed error-message literal, which silently picked up an already-retried row left over from an earlier test run against the same persistent throwaway Postgres container instead of the row it had just inserted — fixed with a per-run unique marker plus an early `break` (list order is newest-first).
    - **Frontend test infrastructure added from scratch** (previously zero devDependencies, zero config, zero test script) — Vitest + React Testing Library + jsdom, per the official Next.js 16 Vitest guide bundled in this exact `next` version's own `node_modules/next/dist/docs/`. Two real environment bugs hit and fixed, both specific to this Windows dev machine, neither a defect in the app code: Vitest's default `forks` pool timed out spawning worker processes because this repo's path contains spaces ("Ritesh Gupta", "FINAL PROJECTS") and the pool's bootstrap mis-escapes it — switched to the `threads` pool; jsdom's `SubtleCrypto.digest` rejected the real `ArrayBuffer` `File.slice().arrayBuffer()` produces with a cross-realm `instanceof` mismatch — stubbed only inside `lib/upload.test.ts` (which tests the resume/fingerprint state machine, not per-chunk hash correctness). Also needed an explicit `afterEach(cleanup)` in `vitest.setup.ts`: without `test.globals: true`, React Testing Library's own auto-cleanup never registers, so a second component test in the same file saw the first test's still-mounted DOM (`getByRole` failing with "found multiple elements").
    - **What got tested**: `lib/sha256-stream.ts`'s hand-rolled streaming SHA-256 — named in `docs/next-session.md` as untested intricate client logic — verified against Node's real `crypto.createHash('sha256')` as an oracle across empty/small/every FIPS-180-4 padding-boundary-adjacent input size (55/56/57/63/64/65 bytes) and multiple chunking strategies (whole, 64-byte-aligned, irregular, byte-by-byte) rather than hand-typed hex constants that risk transcription error. `lib/upload.ts`'s resume/fingerprint logic — the other named gap — tested through `uploadFile`'s observable behavior (which `localStorage` keys result, which mocked `api.uploads.*` calls happen) since `fingerprintFor`/`loadResume`/`saveResume`/`clearResume` are module-private: fresh-upload vs. resumed-upload vs. size-mismatch vs. cross-target-scoping vs. 404/409-drops-the-record vs. transient-error-keeps-the-record, all six paths. `lib/format.ts` (byte/date/relative-time formatting, including the 0-byte special case) and two components — `EmptyState` (roadmap #16) and `ConfirmDialog` (Escape/backdrop/Cancel-close, busy-state, thrown-error-surfaces-via-`role=alert`, the `useModal` a11y contract item 36 built) — round out the 5 files / 42 tests.
    - **CI**: `.github/workflows/ci.yml`'s `integration-test` job gained real MinIO and NATS instances (plain `docker run -d` steps, not `services:` entries — GitHub Actions' `services:` block has no way to pass the `server /data`/`-js` command args either image needs, same reason the pre-existing migrations step was already a raw `docker run` rather than a service) so `gc`/`events`/`sharing`/etc. are exercised on every push, not just locally. `build-test`'s unit-test step and `integration-test`'s own step both gained `-coverprofile`/`-coverpkg=./...` plus a printed `go tool cover -func` summary — no threshold gate, just the number the audit said didn't exist anywhere. `make test-cover`/`make test-integration` added for the same thing locally. The `frontend` job gained an `npm test` (`vitest run`) step; `npm run test:coverage` added locally (`@vitest/coverage-v8`), same no-gate-yet philosophy.
    - **Verification**: `go build`/`go vet ./...`/`go vet -tags=integration ./...` all clean across the whole backend. The full `-tags=integration ./...` suite (all 7 newly-covered modules plus the existing 3) was run against real throwaway Postgres/Redis/MinIO/NATS containers on non-default host ports (15432/16379/19000/14222 — avoiding both the native-Postgres-on-5432 footgun and the already-running `nimbus` kind cluster's NodePorts/`pulseboard-redis`'s 6379, neither of which was touched), green and stable across 3 repeated full runs. Frontend: `npx tsc --noEmit`/`npx eslint .`/`npm run build` all clean, `npm test` green across 3 repeated runs. CI itself was validated with real (non-dry-run) `act` runs: `build-test` and `frontend` both passed end-to-end against the actual runner image, including the new test steps and coverage output; `integration-test` could not be exercised via `act` on this machine — its `run migrations` step (pre-existing, unrelated to this session's changes) hit `act`'s well-known Docker-in-Docker bind-mount limitation, the same class of gap already documented for `helm-smoke-deploy`'s kind-cluster step in item 32 — so the new MinIO/NATS wiring's actual GitHub Actions behavior is unconfirmed until a real push runs it, same caveat item 32/13 already carries for that job.
    - **Not done, disclosed rather than silently skipped**: HTTP handler layers across all 7 newly-tested backend modules remain at 0% coverage (`go test -tags=integration -coverpkg=./... ./...` shows the service/repository logic mostly 70–100%, handlers untested) — this session tested the same layer the audit's own two named targets (`sharing`, `gc`) live in, not every layer. Frontend coverage is real but narrow: `npm run test:coverage` reports ~60% statements only across the 6 files the new tests actually touch (`ConfirmDialog.tsx`, `Icons.tsx` transitively, `api.ts`, `tokens.ts`, `upload.ts`, `useModal.ts`) — most of `app/`, most components, and `lib/download.ts`/`lib/live.ts` have no tests at all. No coverage threshold is enforced in CI either language — the numbers are visible now, nothing fails a push for a coverage drop.

38. (Docker/k8s hardening + repo hygiene session, 2026-08-09, after item 37 — user asked to close every gap the audit's §16 "Docker, Compose & Kubernetes" (7.0/10) and §18 "Repo hygiene & process" (7.5/10) sections named) All fixable gaps closed, live-verified against the real running `nimbus` kind cluster (Compose was not up this session — validated via `docker compose config` instead of a live restart, to avoid the documented "Compose and kind can't run at the same time" conflict):
    - **Container resource limits, everywhere they were missing.** `deploy/docker-compose.yml`: every service gained `deploy.resources.limits` (cpu/memory) — a real, enforced cap under Compose V2 even outside Swarm mode, not just documentation. `deploy/k8s/helm/nimbus/templates/{api,worker,web}-deployment.yaml`: added `resources.limits` alongside the pre-existing `requests`. `deploy/k8s/infra/{postgres,redis,nats,minio,prometheus,grafana,tempo}.yaml`: none of these had *any* resources block before — all seven gained requests+limits. Sizing was live-checked, not guessed: a `kubectl top pods` pass against the running cluster caught Grafana already using ~306Mi against an initially-set 256Mi limit, so that one was raised to 512Mi before anything was applied (both the Compose and k8s values) — the kind of mistake a live check catches that a static review wouldn't.
    - **HPA stub → real autoscaling.** `deploy/k8s/infra/metrics-server.yaml` (new — the real upstream `kubernetes-sigs/metrics-server` release manifest, patched with only `--kubelet-insecure-tls`, the standard documented kind/minikube workaround for kubelet serving certs metrics-server doesn't trust by default) is now applied by `apply.sh` before the Helm chart. **Live-verified, not just deployed**: applied to the running `nimbus` cluster, `kubectl top nodes`/`kubectl top pods` returned real numbers immediately, and `kubectl get hpa` flipped from the long-standing `cpu: <unknown>/70%` to a real `cpu: 5%/70%` within the metrics-server's first scrape window.
    - **Compose port exposure hardened.** Every infra-only service with no reason to be reachable from outside this machine's own containers — Postgres, Redis, NATS, both MinIO console ports (not the S3 API ports), Mailpit, Tempo, Prometheus, Grafana — now binds `127.0.0.1:<port>:<port>` instead of the previous bare `<port>:<port>` (which Compose binds to `0.0.0.0`). This was the audit's standout §16 gap: those ports carry shared dev credentials (`nimbus`/`nimbus`, `nimbus`/`nimbus-secret`) and, prior to this fix, would have been exposed the moment any tunnel or port-forward sat in front of the app ports alongside them. `nimbus-api`/`nimbus-web` and the three MinIO **S3 API** ports (9000/9010/9020) deliberately stay open on all interfaces — those are the actual public surface and what presigned upload/download URLs need to reach from a browser, including through the Cloudflare tunnel setup. The equivalent k8s exposure was already fine without changes: `postgres`/`redis`/`nats`/`tempo` are ClusterIP-only in `deploy/k8s/infra/`, never NodePort'd — this was a Compose-specific gap, matching how the audit's own gap description was worded.
    - **Backup/restore story.** `scripts/backup.sh [compose|kind]` (pg_dump, gzipped) + `mc mirror` of all three MinIO nodes' buckets to `./backups/<timestamp>/` (gitignored); `scripts/restore.sh <dir> [compose|kind]` reverses both, gated behind an explicit `CONFIRM=yes` since it overwrites live data. **A real Windows-specific bug was caught by live-running this, not by writing it**: the first MinIO implementation used a `-v` bind mount into a throwaway `minio/mc` container, which silently failed — Docker Desktop on Windows doesn't correctly translate a `-v` source path containing spaces (`Ritesh Gupta`, `FINAL PROJECTS`), so the container saw `/backup` as a nonexistent directory with no error surfaced, and the mirrored files simply never landed on the host. Same footgun class CLAUDE.md already documents for Vitest's pool spawning, just hitting a new tool. Fixed by switching to a named container + `docker cp` (not a bind mount, unaffected by the path-with-spaces bug) — confirmed by re-running end-to-end against the live kind cluster: a real 24KB Postgres dump (21 tables' worth of `COPY` statements) and real chunk objects mirrored from all three MinIO nodes (9.27/13.98/20.92 MiB, genuine content-addressed filenames) all correctly landed on the host filesystem this time. `restore.sh`'s guard was checked (blocks without `CONFIRM=yes`) and its syntax validated; the actual destructive overwrite path was deliberately not exercised against the live cluster's real data.
    - **`CONTRIBUTING.md`** (new) — closes §18's "no CONTRIBUTING.md" gap. Documents the project's own working conventions (points at `CLAUDE.md`/this doc rather than duplicating them) and states explicitly that feature-branch-plus-PR is the expected pattern *once a second contributor exists* — direct-to-`main` was already correctly identified in the audit as a deliberate, working solo-project pattern, not something this session tried to retroactively change.
    - **Not done, disclosed rather than silently fixed**: the orphaned Cloudflare named-tunnel Windows service (§18's third gap) was left alone — uninstalling a Windows service is a system-settings change, outside what an agent should do unprompted; the user was told to run `cloudflared.exe service uninstall` themselves if they want it gone. It remains exactly as documented: harmless-but-unused.
    - **Verification**: `docker compose -f deploy/docker-compose.yml config` clean; `helm lint`/`helm template` clean; `kubectl apply --dry-run=server` against the real cluster API for every changed manifest (schema-valid, no rejected fields) before anything was applied for real. Then applied for real: all seven infra manifests + `helm upgrade` rolled out clean (`kubectl rollout status` succeeded for every Deployment/StatefulSet, zero pods stuck, zero unexpected restarts), `/healthz`/`/readyz`/`/api/health` all returned 200 through the real kind NodePorts afterward, and `scripts/smoke-auth.sh` (register → login → org → refresh-rotation → reuse-detection) passed clean end-to-end against the post-change cluster — proving actual functionality survived the resource-limit rollout, not just that pods came back `Running`.

39. (Frontend architecture/visual-design/UX session, 2026-08-09, after item 38 — user asked to close every gap the audit's §10 "Frontend architecture" (8.3/10), §11 "Visual design system" (8.0/10), and §12 "UX flows & interaction design" (8.0/10) sections named) All fixable gaps closed, live-verified against the real running `nimbus` kind cluster with a real chunked upload driven through the browser console (no file-picker automation tool available, same documented workaround item 26 used) and real UI clicks through the actual React app:
    - **§12 — optimistic UI updates for rename/move/trash**, the section's own last-named gap ("actions like rename/move/trash still wait for the round-trip and then trigger an SWR revalidation rather than updating local state immediately"). `frontend/components/MoveDialog.tsx`'s `onMoved: () => void` prop became `onMove: (targetFolderId) => Promise<void>` — the dialog no longer calls `api.files.move`/`api.folders.move` itself, it delegates to whichever caller owns the SWR cache the item lives in. `frontend/app/app/org/[orgId]/folder/[folderId]/page.tsx` gained `optimisticTrashFile`/`optimisticRenameFile`/`optimisticMoveFile` (plus the equivalent inline for folder trash/move) using SWR's `mutate(promise, {optimisticData, rollbackOnError: true, revalidate: true, populateCache: false})` — the list updates the instant the user acts, the real request runs in the background, and a rejection rolls the cache back automatically. `frontend/components/FileRow.tsx` calls these via three new props (`onOptimisticTrash`/`onOptimisticRename`/`onOptimisticMove`) instead of hitting `api.files.*` directly. One real UX subtlety worked through, not just typed around: a failed trash can't reliably show its error on the row's own inline `role="alert"` state, because the row already disappeared from the list (optimistic removal) by the time the rejection arrives — SWR's rollback then remounts a *fresh* component instance whose local state never saw the error. Trash failures surface via the existing toast instead (documented in `FileRow.tsx`'s own comment); rename failures still use the inline alert since that row never leaves the list. **Live-verified end-to-end against a real chunked upload** (register → login → the actual chunked-upload API called directly from the browser console, per item 26's precedent) for all three actions: renamed a real file through the UI and confirmed via the network tab that the display update landed before/independent of the PATCH's response, then confirmed a background revalidation GET followed it; trashed a file and confirmed both the instant UI removal and, via a direct API check, that the file was genuinely in the org's trash server-side; moved a file into a new subfolder via the real drill-down `MoveDialog` and confirmed via a direct API check that it landed in the destination folder, not lost. `frontend/components/FileRow.test.tsx` (new, 7 tests) and the `MoveDialog`/`FolderPage` changes are typechecked/linted clean; the rollback-on-error path itself rests on SWR's own documented `rollbackOnError` behavior rather than a hand-rolled equivalent.
    - **§11 — a real light theme**, closing "no light theme at all... worth being a deliberate one rather than an assumed one." `frontend/app/globals.css` gained a full second token set (`@media (prefers-color-scheme: light)` for the OS-default case, `:root[data-theme="light"|"dark"]` for an explicit override) — not a naive inversion: `--accent`/`--accent-strong` stay identical across themes (they're fills/rings/gradients, already clear the 3:1 non-text bar on light), but `--accent-2`/`--danger`/`--success`/`--warning` needed genuine re-picking since they're used as body-sized text/link color and the dark-mode values fail outright on white (warning's dark value is ~1.7:1 on the new light background, computed). Every light-mode text token's worst-case contrast against all four light surfaces is documented in a dated comment the same way the existing dark-mode AA comment already was, worst case 4.69:1 (§16-style rigor, not eyeballed). `frontend/lib/theme.ts` (new) + a `beforeInteractive` `<Script>` in `app/layout.tsx` apply a stored override before first paint (avoiding a flash of the wrong theme) via `useSyncExternalStore` in the new `frontend/components/ThemeToggle.tsx` — not `useState`+`useEffect`, which this codebase's own `react-hooks/set-state-in-effect` lint rule correctly flagged as the anti-pattern it is; `useSyncExternalStore` is React's actual answer to "client-only value, no hydration mismatch." The toggle lives in both `AppShell` nav bars (desktop sidebar footer, mobile top bar). **Live-verified**: clicked the toggle and confirmed via computed styles that `--background`/`--foreground`/`--muted-2`/`--accent-2` all flipped to the real light values, `color-scheme` flipped to `light`, the choice persisted in `localStorage` and survived a real page reload with no flash (the `beforeInteractive` script had already applied it before the first paint).
    - **§11 — font fallback documented**, this section's other named gap. Turned out to already work (Next's `Mona_Sans()` loader self-hosts the font and generates a metrics-matched local fallback, plus `globals.css`'s own `body` rule already chains `ui-sans-serif, system-ui, sans-serif` after it) — the gap was that this wasn't confirmed or written down anywhere. Live-verified by overriding `--font-mona-sans` in devtools to an unavailable font name and confirming the computed `font-family` genuinely falls through the rest of the chain (a live `var()` resolution, not a value Tailwind's `@theme inline` baked in statically) — now a comment on `globals.css`'s `body` rule.
    - **§11 — a dev-only kitchen-sink route**, the section's own suggested fix ("even a minimal Storybook-less 'kitchen sink' route... would pay for itself fast"). `frontend/app/dev/kitchen-sink/` renders every `components/ui/` primitive in every state — all 4 `Button` variants × default/disabled, all 4 `Badge` tones, `Input` empty/filled/disabled, `Checkbox` interactive/disabled, `Card`/`EyebrowLabel`/`StatCard`, `PageHeader` with/without description+actions, a real `TablePanel`/`Th`/`Td`/`Tr` table with a keyboard-operable clickable row, `EmptyState` with/without an action, and all 23 icons in a labeled grid — plus the new `ThemeToggle` so both themes can be checked against every component in one place. Gated with `if (process.env.NODE_ENV !== "development") notFound()`, the standard Next.js pattern for a route that should compile (so a real `npm run build` still catches it breaking) but never be reachable on a production deployment — confirmed by `npm run build`: the route prerenders statically and no link to it exists anywhere in the app's own nav.
    - **§10 — direct tests for `lib/api.ts`'s request/refresh-race/error-mapping logic**, the section's own named highest-value gap (previously only exercised indirectly through `lib/upload.ts`'s tests). `frontend/lib/api.test.ts` (new, 9 tests) covers the Authorization-header/response-parsing happy path, error-code/message mapping with and without a server-supplied `error` field, the 204-no-body case, and — the one genuinely tricky piece — a regression test proving concurrent 401s share exactly one `refresh()` call rather than each triggering their own rotation of the same refresh token (which the code's own comment already named as a real, easy-to-reintroduce bug: the loser's reuse of an already-rotated token revokes the whole family server-side).
    - **§10 — component tests for the file browser and the upload entry point**, this section's other named highest-value gap ("component tests for the pages that actually drive most user interaction"). `frontend/components/FileRow.test.tsx` (new, 7 tests) covers expand-and-load-versions, the rename/trash optimistic-prop contract above, the toast-vs-inline-alert failure-surfacing split, and the Undo action. `frontend/components/UploadDropzone.test.tsx` (new, 6 tests) covers file-picker selection, drag-and-drop, the done/error progress states, and keyboard operability (Enter opens the file picker) — `lib/upload.ts` itself was already tested (item 37); this is the component wrapped around it.
    - **Verification**: `npx tsc --noEmit` and `npx eslint .` both clean across every changed/new file (0 new errors; two pre-existing unrelated warnings in `coverage/` and `lib/useModal.ts` untouched). `npm run build` succeeds, `/dev/kitchen-sink` confirmed statically prerendered. `npx vitest run` — full suite, not just the new files — is 8 files/64 tests green (up from 5 files/42 tests before this session). Live verification used the real `nimbus` kind cluster (already up from item 38's session) via the frontend's own dev server pointed at it (`NEXT_PUBLIC_API_URL=http://localhost:8080`); a throwaway `theme-verify-<timestamp>@nimbus.dev` account, its auto-created "Theme Verify Org," a "Destination" subfolder, and one moved test file are left in that Postgres volume, not cleaned up — same disclosed-leftover-test-data pattern earlier sessions (item 26's `a11y-audit-test@nimbus.dev`, item 34's `trace-test@nimbus.dev`) already established. **Not done, disclosed rather than silently skipped**: §12's optimistic-mutate implementation itself (the `mutate()` calls in `FolderPage`) has no dedicated unit test — it's exercised by the live browser verification above and by `FileRow.test.tsx`'s coverage of the *contract* FileRow has with it (that the right prop gets called with the right arguments), but not by a test that mocks SWR's cache directly; §10's own "Still worth knowing" note about `lib/api.ts`'s ad hoc React Context (no single client-state store) was left as an architectural observation, not something this session tried to restructure, matching the note's own framing that it's "fine at this size."

40. (CI/CD-caching + database-usage-counter + testing session, 2026-08-09, after item 39 — user asked to improve the audit's three lowest-scored sections: §15 CI/CD (8.3), §06 Database design (8.4), §14 Testing (8.5)) All three genuinely moved, plus one real pre-existing bug found and fixed along the way:
    - **§15 — Docker layer caching, closing the "doubling the from-scratch build cost per push" gap.** `docker-build` and `helm-smoke-deploy` (which independently rebuilds the identical four Dockerfiles) both now build via `docker/setup-buildx-action` + `docker/build-push-action` with the GitHub Actions cache backend (`type=gha`), keyed by image name (`scope: nimbus-api`/`nimbus-worker`/`nimbus-web`/`nimbus-migrate`) rather than by job — a layer either job populates is reusable by the other. Verified for real (not just YAML-valid) by running the `docker-build` job through `act` end to end: all four images built and loaded successfully.
    - **§15 — real coverage-regression floors in CI**, closing "nothing fails a push for a coverage drop yet." `build-test`'s unit-coverage step and `integration-test`'s integration-coverage step each now fail the push if `go tool cover`'s total drops below a floor set just under the real measured baseline at the time (unit 4.0%, integration 20.0% — measured 4.7%/22.7% against the live `nimbus` kind cluster's Postgres via a `kubectl port-forward`; both use `-coverpkg=./...`, so the total reads far lower than any single well-tested package's own number, since it also counts untested `cmd/*` binaries and thin `platform/*` helpers).
    - **§06 — `OrgUsageBytes` is now a maintained counter, not a live `SUM` join**, this section's own suggested fix. New migration `000015_org_usage_counter` adds `organizations.usage_bytes bigint NOT NULL DEFAULT 0 CHECK (>= 0)`, backfilled from the real data. `file.Repository.CreateWithVersion`/`AddVersion` increment it inside their existing transactions; `file.Repository.Purge`/`PurgeExpiredTrash` (now transactional, previously a bare `DELETE`) sum the freed bytes *before* the cascade removes `file_versions`, then decrement. The non-obvious part: **deleting a `folders` row cascades straight through `files`/`file_versions` without ever calling `file.Repository`** — `folder.Repository.PurgeExpiredTrash`'s trash-retention sweep is a second, independent write path to the same counter that would have silently drifted the counter upward forever if left alone, so it got the identical guard-then-sum-then-delete-then-decrement transactional treatment (new `purgeOneFolderCascade` helper). All four write paths have direct integration-test coverage, including a new `TestPurgeExpiredTrash_CascadeDecrementsOrgUsageBytes` in `folder/repository_integration_test.go` proving the folder-cascade path specifically — the one write path nothing else would have caught a regression in.
    - **A real, pre-existing test-isolation bug found and fixed in the process**: making `Purge`/`PurgeExpiredTrash` correctly transactional added just enough extra DB round-trips that `TestPurgeTrash_ExpiredFileIsAutoPurgedAndFeedsMark` (`internal/gc`) started intermittently failing (`doomed=2, want 1`) when run as part of the full package suite, though it always passed in isolation. Root-caused (not just retried away) by diffing pre/post-change behavior against the same live database: `TestResurrection_RecommittingADoomedChunkFlipsItBackToLive` deliberately resurrects a chunk to `gc_state='live'` with a fresh `last_seen_at` but never gives it a new `file_version_chunks` reference — that's the whole point, proving `UpsertGlobalChunk` alone does it — but left alone, it's a real live-and-unreferenced row sitting in the shared test database. `fx.sweeper`'s grace period is only 1 second, so once enough wall-clock time passed (four earlier tests plus the extra round-trips), a *later* test's `mark()` call (a global, unscoped `UPDATE`) swept it in and inflated that test's own count. Fixed with an explicit `t.Cleanup` deleting the resurrected chunk row — verified stable across 3 repeated full-suite runs after the fix, versus reliably reproducing before it (confirmed by literally `git stash`-ing the change and re-running against the identical database).
    - **§06 — `EXPLAIN ANALYZE`-validated the two newer activity aggregate queries** (`ActorStats`/`VerbCounts`), the section's other named gap ("plausible they're fine... but nothing in the docs confirms it was checked"). The real data was too small (≤5 rows/org) for a meaningful planner decision, so seeded a synthetic 150k-row spread across all 172 real orgs (array-indexed per-row selection, not a naive uncorrelated subquery — the first seeding attempt collapsed to a single InitPlan-evaluated org id, a real gotcha worth remembering), ran `EXPLAIN (ANALYZE, BUFFERS)` against a realistically-populated org (both confirmed using `idx_activity_org_time` via a Bitmap Index Scan, 2.8–5.8ms, not a sequential scan), then deleted every synthetic row and re-`ANALYZE`d the table back to its real 35-row state. Findings documented as dated comments on both functions in `activity/repository.go`.
    - **§14 — real HTTP-handler-layer tests for `sharing`**, the audit's explicitly-named target module. The existing `Delete` handler tests already exercised the real `sharing.Handler` (not just the service layer); `CreateBundle`/`Resolve` didn't — three new tests in `sharing/repository_integration_test.go` close that: empty-`file_ids`-returns-400, cross-org-file-returns-400 (both through the handler's own JSON-parse-then-service-error-switch, not `fx.svc` directly), and a full create-then-resolve round trip asserting the real JSON response bodies a client would actually receive.
    - **§14 — frontend coverage threshold: investigated, not added, and why is documented in code.** `npm run test:coverage` (`vitest run --coverage`) cannot currently complete at all — every worker (pool `threads`/`forks`; `isolate` true/false; `fileParallelism` true/false; `maxWorkers=1`) times out its startup handshake at exactly 60s the instant `--coverage` is added, reproduced identically on Windows, Alpine (musl), and Debian (glibc) with ample CPU/RAM — ruling out the pool footgun already documented in `vitest.config.mts` (that one only affects `pool:"forks"`+paths-with-spaces; this happens on `"threads"` too, in a container with no spaces in its path). Matches a known, still-open upstream Vitest 4 pool-implementation bug class (`vitest-dev/vitest` issues #8766, #8968, #8861, #9494) at the exact installed `vitest@4.1.10`. `npm test` (no coverage) is unaffected — confirmed 8 files/64 tests still green after this investigation. Documented directly in `vitest.config.mts`'s `coverage` block so a future session doesn't re-discover this from scratch; **do not add a frontend coverage CI step without first re-verifying against a newer vitest**, or CI will hang/fail on every push.
    - **Known-issue bullets below updated to reflect this session's confirmation**: `helm-smoke-deploy` and `integration-test`'s MinIO/NATS steps have now both actually run green on a real GitHub-hosted runner (CI run `31305181188`, triggered by this session's separate `helm-smoke-deploy` connection-reset fix, landed before this session's own three-section work) — the "unconfirmed on a real runner" caveat on both is resolved.
    - **Verification**: `go build ./...`/`go vet ./...`/`gofmt` clean (module-relative; 5 edited files show as CRLF-vs-LF-mixed in a raw `gofmt -l` purely from this Windows working tree's `core.autocrlf=true` checkout behavior — the git index already stores LF for all of them, confirmed via `git ls-files --eol`, so the committed content is unaffected). Full `go test -tags=integration ./...` and `go test ./... -short` both green against the live `nimbus` kind cluster's Postgres (via `kubectl port-forward`) — run three times post-fix to confirm the gc flake's resolution wasn't luck. `act`-verified `docker-build` end to end. Migration `000015_org_usage_counter` was applied directly to the live `nimbus` kind cluster's Postgres (via `migrate/migrate:v4.17.1` against a `kubectl port-forward`'d connection) so the integration tests above ran against real cluster data, not a throwaway database — `organizations.usage_bytes` exists there now, backfilled. The cluster's application images were rebuilt/reloaded/restarted in a same-day follow-up (all four images, `helm upgrade --force-conflicts` — the HPA owns `nimbus-api`'s `.spec.replicas` via the scale subresource, an expected server-side-apply conflict, now helm revision 7), so `nimbus-api`/`nimbus-worker` are running this session's counter logic for real, not just the schema.

41. (Per-org quota override session, 2026-08-09, same day as item 40 — user pushed further on §06 Database design after item 40, "its so low") A genuinely new gap found by re-reading the schema/config with fresh eyes, not something the audit itself named: `NIMBUS_ORG_QUOTA_BYTES` was a single global config value enforced identically against every org, with no per-tenant differentiation possible anywhere in the data model — a real production system needs to be able to grant one org a bigger (or smaller) limit than the default, not just one number for everyone.
    - **New migration `000016_org_quota_override`** adds `organizations.quota_bytes_override bigint` (nullable — `NULL` means "no override, use the configured default"; `CHECK (... IS NULL OR ... > 0)`).
    - **`org.Repository.QuotaOverride`/`SetQuotaOverride`** are the read/write ends; **`org.Service.EffectiveQuota`** resolves the real number (override if set, else the configured default) and **`org.Service.SetQuota`** is the write-side service method. `org.Service.Usage`'s `Storage.QuotaBytes` now calls `EffectiveQuota` instead of reporting the flat configured value — the usage view and enforcement now can never disagree about what an org's real limit is.
    - **`upload.Service.checkQuota`** (the actual enforcement path) gained a new `QuotaReader` port, satisfied directly by `org.Repository` (no adapter needed, same pattern as `UsageReader`/`FileCreator`) — resolves the override before falling back to `orgQuotaBytes`. Nil-checked so callers that don't care about overrides (`quota_test.go`'s white-box `Service` literals) keep their exact pre-existing behavior with zero changes.
    - **New endpoint**: `PATCH /v1/orgs/{orgId}/quota` — deliberately **platform-admin gated** (`requirePlatformAdmin`, stricter than `GET .../usage`'s org-admin tier), because a cross-tenant limit change is a platform operation, not something an org's own admin/owner should be able to grant itself. `docs/openapi.json` and `frontend/lib/api-schema.generated.ts` regenerated to include it (both CI staleness checks re-verified clean).
    - **Tests**: `TestInitUpload_PerOrgQuotaOverrideBeatsTheConfiguredDefault` (upload — proves a tighter override actually gets enforced instead of the looser default, then that clearing it falls back correctly) plus six new `org` tests spanning repository (`QuotaOverride`/`SetQuotaOverride` round-trip, unknown-org 404), service (`EffectiveQuota` passthrough into `Usage()`), and handler layers (400 for non-positive values, 204 set-then-null-clear round trip through the real handler, 404 for an unknown org) — the handler tests exercise `org.Handler.SetQuota` directly via `httptest`, not just the service call, matching item 40's `sharing` handler-test precedent.
    - **Verification**: `go build`/`go vet ./...` clean, `gofmt -l` shows no new files (only the same 5 CRLF-working-tree files item 40 already explained). Full `go test -tags=integration ./...` and `go test ./... -short` both green against the live `nimbus` kind cluster's Postgres. Migration `000016` applied directly to that live database. Frontend `npx tsc --noEmit`/`npm run lint` both clean against the regenerated schema (0 new lint problems; the same 2 pre-existing unrelated warnings item 39 already noted). **`nimbus-api` rebuilt and redeployed this session** (worker/web/migrate didn't need it — neither imports `org`/`upload`) — live-verified via the real Members/Usage page: an org with no override still correctly shows "5.0 GB" (the configured default), proving `EffectiveQuota`'s fallback path is exactly backward-compatible, not just unit-tested. No frontend UI was added for setting the override (platform-admin tooling for it doesn't exist yet — the endpoint is real and tested, but there's no `/admin` page wired to call it) — disclosed as a natural next step, not built speculatively.
    - **The standalone audit report itself (item 26's artifact, not a doc in this repo) was revised same-day (12th pass)** to reflect items 40–41: §06 Database design 8.4 → 9.2 (both its own named gaps closed, plus the per-tenant quota gap above that the audit had never named), §14 Testing 8.5 → 8.9 (coverage floors + `sharing`/quota-endpoint handler tests, honestly still short of full handler-layer breadth), §15 CI/CD 8.3 → 9.4 (Docker layer caching, the `helm-smoke-deploy` fix proven on a real runner, branch protection). Overall 9.3 → 9.4. This doc edit and that artifact edit are two separate surfaces kept in sync by hand — this repo has no automation linking them.

42. (Audit close-out session, 2026-08-09, after item 41 — user asked to close every remaining item the standalone audit report listed as "worth knowing," except the deployment/VPS-provisioning items already deferred to the go-public plan) Worked through the audit's own remaining punch list: handler-layer test coverage for the four backend modules still at 0% (§14), and the per-org quota-override endpoint's missing frontend UI (§06 item 41's own "not built yet" note).
    - **Handler-layer tests for `org`, `folder`, `file`, `search`** — the audit named these as the last modules with real HTTP-contract gaps even after item 37/40's service-layer work. 26 new tests total, all via `httptest` against the real `Handler` type (not just the service), following the exact pattern item 40 set for `sharing`/`org.SetQuota`: `org` (12 — `ListMembers`, `AddMember`'s email/role-elevation/duplicate/success paths, `RemoveMember`'s owner-protection/admin-bound/success paths), `folder` (7 — `Create`, `ListChildren`, `Path`, `Update`'s rename-and-cyclic-move-rejection, `Delete`, `Restore`'s non-member-403), `file` (4 — `Update`'s rename-and-cross-org-folder-rejection, `Delete`, `Restore`+`Purge`'s non-member-403-then-lifecycle, `ListVersions`), `search` (3 — `Search`'s invalid-`date_from`/`size_min` 400s and a real JSON-response-body round trip). `org`'s `DownloadPlan`/`ThumbnailTargets` and `file`'s equivalents stay out of scope, matching item 37's own documented carve-out (need a real `storage.Router`/MinIO).
    - **Quota-override admin UI built** — closes the one disclosed gap from item 41 ("the endpoint is real and tested, but there's no page wired to call it"). `frontend/lib/api.ts` gained `orgs.setQuota`; the Members page (`app/app/org/[orgId]/members/page.tsx`) renders a "Platform admin — per-tenant quota override" panel gated on `me.is_platform_admin` alone (not on the owner-gated `usage` fetch succeeding, since a platform admin auditing an org they don't own would otherwise never see the control) — a GB-denominated input plus Set/Clear, reusing the existing `usage` SWR key so the stat cards above it refresh with the new effective quota on success.
    - **Stale UI copy fixed in passing**: `app/app/org/[orgId]/admin/page.tsx`'s non-admin explainer still said access was "granted per account via NIMBUS_PLATFORM_ADMIN_EMAILS" — three sessions after item 25 replaced that mechanism with the single seeded `NIMBUS_ADMIN_EMAIL` account. Corrected to match.
    - **Investigated and confirmed still-blocked, not re-attempted**: the frontend Vitest coverage-threshold gap (item 37/40's disclosed upstream bug). `npm view vitest versions` shows `4.1.10` (already installed) is still the newest stable release — 5.x exists only as betas (`5.0.0-beta.1`–`.7`). Per `vitest.config.mts`'s own comment ("don't add a coverage step without re-verifying this first"), no newer stable fix exists yet, so nothing was changed.
    - **Deliberately out of scope this session** (user excluded deployment work): rotating the shared Compose/kind dev credentials, and the orphaned Cloudflare named-tunnel Windows service — both already correctly characterized as VPS-provisioning/go-public-plan items in "Known issues" below, not code gaps.
    - **Verification**: `go build ./...`/`go vet ./...`/`go vet -tags=integration ./...` all clean. All 26 new handler tests plus the full existing `org`/`folder`/`file`/`search` integration suites run live against the real `nimbus` kind cluster's Postgres (`kubectl port-forward svc/postgres`) — green, no regressions (same 5-file CRLF-only `gofmt -l` noise items 40–41 already explained, confirmed unchanged). Frontend: `npx tsc --noEmit`/`npm run lint` clean (same 2 pre-existing unrelated warnings). The quota UI was live-verified end-to-end against the real kind cluster's `nimbus-api` (not the stale in-cluster `nimbus-web` image — ran `npm run dev` locally against `NEXT_PUBLIC_API_URL=http://localhost:8080`, the same NodePort the cluster already exposes): registered a fresh account, confirmed the panel is correctly absent for a non-platform-admin owner, promoted that one test account to `is_platform_admin` directly in the live DB, reloaded, confirmed the panel now renders with the real effective quota (5.0 GB default), set a 2 GB override and confirmed both the UI *and* a direct `psql` query against `organizations.quota_bytes_override` showed `2147483648`, cleared it and confirmed both the UI and the DB fell back to the default — then revoked the test account's `is_platform_admin` flag again to avoid leaving a stray promoted account on the shared dev cluster. Note for next session: the seeded admin login didn't work with either `deploy/.env`'s `NIMBUS_ADMIN_PASSWORD` or the live cluster's Helm secret value (`Dev-Admin-Pass1-ChangeMe!`) — both 401'd, consistent with the documented promote-only/no-rotation-on-existing-account behavior (see "Known footguns" in `CLAUDE.md`); the cluster's real admin credential is neither of those two recorded values.

43. (Admin credential reset, 2026-08-09, immediately after item 42 — user asked to actually resolve item 42's flagged mismatch) Root-caused via `helm history nimbus -n nimbus` + `helm get values --revision N`: this cluster's seeded admin account was first *created* under helm revision 3's `adminPassword` (`dev-only-admin-pass-change-me`, deployed 2026-08-08 — the upgrade that first shipped the seeded-admin mechanism to this cluster), not revision 4–7's current chart default (`Dev-Admin-Pass1-ChangeMe!`) or `deploy/.env`'s value — matching `EnsureSeededAdmin`'s documented promote-only behavior (password only set on first creation, never on a later conflict). Confirmed by logging in with the revision-3 value directly against `nimbus-api` (200, real token).
    - **Reset, not recovered**: checked the row had zero references anywhere a `NO ACTION` FK would block a delete first (`organizations.owner_user_id`, `files/file_versions/share_links/uploads.created_by`, `activity_events.actor_user_id` — all 0; only `refresh_tokens`/`auth_audit_log` referenced it, both `ON DELETE CASCADE`), then `DELETE FROM users WHERE email = 'admin@nimbus.dev'` directly against the live cluster's Postgres and `kubectl rollout restart deployment/nimbus-api -n nimbus`. `EnsureSeededAdmin` re-ran on the new pod's startup (`cmd/api/main.go`) and recreated the account under the *current* Helm secret value.
    - **Verified live**: `admin@nimbus.dev` / `Dev-Admin-Pass1-ChangeMe!` (the value currently in both `values.yaml`'s chart default and the live Helm secret) now logs in — 200, `GET /v1/auth/me` confirms `is_platform_admin: true` on the freshly-seeded account. This is now the cluster's real working admin login; the old revision-3 password no longer works (the row it belonged to is gone).

44. (Vercel frontend deploy, 2026-08-09, after item 43 — user wants the frontend on Vercel now, VPS backend deferred to "someday") A pre-existing but never-completed Vercel link (`frontend/.vercel/project.json`, a stray `VERCEL_OIDC_TOKEN` in `.gitignore`d `frontend/.env.local`) turned out to belong to a project the connected Claude/Vercel MCP integration can't see (`get_project`/`list_projects` both came back empty against the exact `projectId`) — administered instead via the authenticated `vercel` CLI (`npx vercel`, already logged in as the user's real account) using the ID/org pulled straight from that `.vercel/project.json` file.
    - **Root cause of the failing GitHub-triggered build**: the Vercel project's Root Directory setting was unset, so it looked for `package.json` at the repo root instead of `frontend/package.json` (this is a `backend/`+`frontend/` sibling monorepo, not a single-package repo). Fixed via `vercel project update nimbus-storage-platform --root-directory frontend --framework nextjs`.
    - **A second, only-visible-once-the-first-was-fixed build failure**: `next.config.ts`'s `output: "standalone"` (added for `deploy/Dockerfile.web`'s self-hosted build) broke Vercel's own post-build step (`ENOENT` on `next-server.js.nft.json` — standalone mode's trace-file layout isn't what Vercel's packaging step expects). Fixed by gating it on the `VERCEL` env var Vercel sets on every build: `output: process.env.VERCEL ? undefined : "standalone"` — the Docker path is unaffected since that env var is never set there.
    - **Verified with a real production deploy**, not just a passing build: `vercel --prod` (run from the repo root with `VERCEL_ORG_ID`/`VERCEL_PROJECT_ID` env vars standing in for the project link, since `.vercel/project.json` lives inside `frontend/` and CLI-deploying from inside that directory uploads only its contents — a mismatch with the Root Directory setting that only matters for local CLI deploys, not GitHub-triggered ones) completed clean and is live at **https://rit-nimbus.vercel.app**. Confirmed rendering for real (login page loads, no console errors) via a live browser check.
    - **Known limitation, not a bug**: the deployed frontend can't actually log in yet — `NEXT_PUBLIC_API_URL` is still baked to `http://localhost:8080` and `nimbus-api`'s `NIMBUS_CORS_ORIGIN` is single-origin, still pointed at local dev. Both need real values once the backend has a public address; deliberately not touched this session since the backend isn't deployed anywhere reachable from Vercel yet (that's the deferred VPS work). The frontend-only Vercel deploy was an explicit, scoped ask — not an expansion of the go-public plan.

## Known issues / gaps (real, not hypothetical — flag before treating any of this as done)

- ~~Backend is not publicly reachable~~ — **superseded 2026-07-07**: the original plan (paid VPS, Hetzner/DO) was dropped when the user asked for a genuinely $0/month path instead. Oracle Cloud's Always Free Ampere tier was attempted first and abandoned mid-setup — its instance-creation cost estimator kept showing a non-zero charge for boot volume even at default settings (likely just doesn't reflect Always Free credits in its UI, but not worth the ambiguity) — so the user chose to **self-host on this dev machine** instead, exposed via **Cloudflare Quick Tunnels** (`cloudflared`, installed via winget at `C:\Program Files (x86)\cloudflared\cloudflared.exe`). A persistent **named tunnel was also attempted and abandoned**: Cloudflare's tunnel routing (both the dashboard's "Add a route" wizard and the CLI's `tunnel login`) hard-requires an owned domain delegated to Cloudflare as a zone — there's no way around it for a fixed hostname, and genuinely-free full-domain registration (old Freenom-style `.tk`/`.ml`) is unreliable/discontinued. Landed on **ephemeral Quick Tunnels** as the pragmatic $0 answer: ports 3000 (web), 8080 (api), 9000/9010/9020 (the 3 MinIO nodes) each get their own `cloudflared tunnel --url` process and a random `*.trycloudflare.com` hostname. Verified end-to-end for real (not just curl-to-localhost): register → login → create org/folder → chunked upload replicated to 2 MinIO nodes → download, all through the public tunnel hostnames, checksums matched. Key wiring: `NIMBUS_STORAGE_NODES`' public-endpoint half now points at the MinIO tunnel hostnames (config.go's `PublicEndpoint`/`presign.go` correctly derive `Secure: true` from `https://` scheme, no code change needed); `deploy/docker-compose.yml`'s `NIMBUS_STORAGE_NODES` line was changed to `${NIMBUS_STORAGE_NODES:-<same old localhost default>}` so local dev is unaffected and the override lives only in `deploy/.env` (gitignored); `nimbus-web`'s image was rebuilt with `--build-arg NEXT_PUBLIC_API_URL=<api tunnel URL>` since that's a Next.js build-time value, not runtime. **The real limitation**: Quick Tunnel hostnames are random and change every time `cloudflared` (or this machine) restarts — there is no fixed URL. `scripts/start-public-tunnels.ps1` (new) relaunches all 5 tunnels and prints the exact follow-up commands (update `deploy/.env`, restart `nimbus-api`/`nimbus-worker`, rebuild+restart `nimbus-web`) needed after any restart. An orphaned Cloudflare named-tunnel object (`nimbus`, installed as a Windows service via `cloudflared.exe service install <token>`) was left behind mid-pivot — it's inert/harmless but unused; fine to `cloudflared.exe service uninstall` (needs an elevated prompt) if it's ever annoying, not urgent.
- ~~No rate limiting~~ — **built in the Tier 2 session** (item 18): per-user/per-IP Redis token bucket, 25 rps/burst 50 defaults, fail-open.
- ~~No DLQ remediation~~ — **built in the Tier 2 session** (item 18): dead_events table + admin list/retry endpoints + UI panel. Remediation is human-triggered retry, intentionally — no automated replay, no alerting on new dead events yet (Grafana panel would be the natural next step if wanted).
- ~~`GET /v1/admin/orgs/{orgId}/usage` was documented in early API drafts but never implemented~~ — **built in the governance session** (item 22) as `GET /v1/orgs/{orgId}/usage` (owner-gated org governance; the `/v1/admin/` prefix now specifically means platform-admin cluster ops).
- **`internal/admin` module never materialized** — cluster-ops reads live in `storage.Handler`/`events` and org governance lives in `org`; with both now properly gated this split looks permanent, not a gap.
- ~~HPA is a stub~~ — **built item 38**: `deploy/k8s/infra/metrics-server.yaml` (upstream manifest + `--kubelet-insecure-tls` for kind) is now applied by `apply.sh` before the Helm chart, so `kubectl get hpa` reports a real CPU percentage and `nimbus-api` can actually scale 1→3 replicas. Live-verified against the running `nimbus` kind cluster.
- **`scripts/chaos-node-kill.js` and `scripts/load-upload.js` are deliberately not in CI** (decided explicitly with the user on Day 14, not an oversight) — both need real multi-container infra (Compose/kind) and real wall-clock time (~40s for the load test alone), a different cost/value trade-off than a per-push gate. They stay manual/local verification tools; `.github/workflows/ci.yml` runs vet/build/unit/integration/lint/docker-build only.
- ~~Helm chart isn't in CI either~~ / ~~kind-cluster portion unconfirmed on a real GitHub-hosted runner~~ — **built item 32, confirmed live item 40**: a `helm-smoke-deploy` job runs `helm lint`/`helm template` then a real `kind` create → image build/load → `apply.sh` → `helm install --wait` → health-endpoint curl, failing the push if the chart doesn't actually reach a healthy cluster. Item 40 root-caused and fixed its one real flake (a fresh pod's NodePort route resetting the first connection while kube-proxy propagates iptables rules — `curl --retry-connrefused` doesn't cover that error, `--retry-all-errors` does) and confirmed green on a real GitHub Actions runner (run `31305181188`), then added it to branch protection's required-checks list alongside the original four.
- ~~`integration-test`'s new MinIO/NATS steps (item 37) are unconfirmed on a real GitHub-hosted runner~~ — **confirmed item 40**: same CI run (`31305181188`) that proved out the `helm-smoke-deploy` fix also ran `integration-test` end to end on a real Linux runner, green in 55s.
- ~~Frontend has zero automated test coverage~~ / ~~backend test coverage thin against 9,400 lines/14 modules~~ — **built item 37**: Vitest + Testing Library on the frontend (5 files/42 tests, incl. `lib/upload.ts`'s resume state machine and `lib/sha256-stream.ts` verified against Node's real crypto), plus real Go test files for `org`/`sharing`/`folder`/`file`/`gc`/`search`/`events` (previously zero) on the backend. Both narrower than "full coverage" — see item 37's own "Not done" note (HTTP handlers untested backend-side, ~60% of only the touched files frontend-side) — but the audit's roadmap #5 (the last open row) is closed, and the "nothing catches a regression here" gap for `upload.ts`/`sha256-stream.ts`/`gc`/`sharing` specifically is not.
- ~~FR-4 (auth audit log) doesn't exist~~ — **built Day 15**, see Days 1-14 list item 15.
- ~~FR-8's read-side checksum verification is client-facing only~~ — **built Day 15** for the worker's reassembly path (`processing.Processor.reassemble`); the direct-to-client download path (`file.Service.DownloadPlan`) is still client-verified only — a documented architectural fact (server never touches those bytes), not a gap — but as of item 31 the client verification is real: `frontend/lib/download.ts`'s `assembleFromPlan` re-hashes every fetched chunk against the plan's content hash and falls back to the next replica on mismatch, rather than trusting whatever the first successful HTTP fetch returned (which is what it did before). See docs/01-srs.md FR-8.
- **No branch-protection-driven scan for the chaos/load scripts' Compose-only assumptions.** Discovered on Day 15: `scripts/chaos-node-kill.js` and `scripts/smoke-folders.sh`/`smoke-thumbnails.js`'s DB-check steps all hardcode `docker compose`/`docker exec <container-name>`, which has no equivalent once MinIO/Postgres run as `kind` pods rather than Compose containers. README's demo-script cross-environment claim was corrected for step 3; the smoke scripts' kind-incompatibility wasn't itself in scope for a README fix (they're not cited by name in the demo script) but is worth knowing before assuming any script marked "for the API-only version" works against `kind`.
- **README's "<10 minutes" walkthrough claim wasn't stopwatch-verified on Day 15** — the only available environment was the long-running `kind` cluster, not a cold Compose start, and the user chose a careful re-read over disrupting it. The demo steps themselves are fast; a genuinely cold `docker compose up --build` (3 images built from source, no pre-published alternative) is the real variable cost and could push a first-ever run close to or past 10 minutes on a slow connection/machine. Worth an actual timed run next time Compose is started fresh.
- **Mailpit is Compose-only** — `deploy/k8s/infra/` has no mailpit manifest and the Helm chart doesn't set `NIMBUS_SMTP_ADDR`, so on kind the password-reset flow falls back to logging the reset link in `nimbus-api`'s output instead of sending email (a deliberate fallback in `auth.Service.ForgotPassword`, not a crash). Add a manifest + env var if email-on-kind ever matters.
- **Compose and kind can't run at the same time.** Both bind the same host ports (8080, 3000, 9000/9010/9020, 9090, 3001) by design (so the same URLs work in either environment) — a real constraint, not an oversight, but worth knowing before assuming both are up.

## Important design decisions (confirmed, deliberate, worth knowing before touching related code)

- **2-state circuit breaker, not 3-state with half-open** (`backend/internal/storage/breaker.go`) — the fixed 2s health-check cadence already acts as the half-open trial, so a third state added complexity without adding behavior. Don't "fix" this to 3-state without re-reading docs/04-lld.md §1.3 first.
- **Health state is read in-memory, never from Redis, on the hot path** (`Router.Resolve`/`IsHealthy`). Redis is write-only from the health loop's perspective, used only for cross-process visibility (a hypothetical second `nimbus-api` replica, or the admin view). Don't add a Redis read to the resolve path — that would reintroduce the network round-trip the design specifically avoids.
- **`activity` writes are mostly synchronous**, not purely worker-driven — e.g. `uploaded` is recorded directly in `upload.Service.CompleteUpload`. Only genuinely async, processing-derived events (`thumbnail_generated`) come from the worker. See docs/03-hld.md §1 revision note.
- **Standalone MinIO nodes, not MinIO distributed mode** — deliberate, because the replication/placement/failover logic being hand-rolled in `internal/storage` *is* the point of the project. Don't swap in MinIO's built-in clustering as a "simplification"; it would remove the thing being demonstrated.
- **CORS is wildcard (`NIMBUS_CORS_ORIGIN=*`) by design**, not an oversight — safe here because auth is Bearer-token, not cookie-based, so there's no ambient credential a wildcard origin could steal.
- **JWT is HS256, not RS256/JWKS** — fine for a single-service monolith; would need to change if `auth` is ever split into its own deployable.
- **`nimbus_storage_node_healthy` exists as two independent series, one per process** (`backend/internal/storage/router.go` probeOne), not a single shared gauge — a direct consequence of health state being deliberately in-process-only (see the Redis bullet above). `nimbus-api` and `nimbus-worker` each run their own probe loop and each report their own view; Prometheus's `job` label is what tells them apart on the storage-health dashboard. Don't "fix" this into one gauge — that would require introducing the shared-state read the design specifically avoids. As of the architecture-gap session, a Grafana alert (`nimbus-storage-health-disagreement`, `deploy/observability/grafana/provisioning/alerting/rules.yml`) watches for the two series genuinely, persistently disagreeing (`max - min > 0` for 2+ minutes) and pages if so — the two-gauge design stays, but a lasting split between them no longer goes unnoticed.
- **kind's NodePort mappings intentionally reuse Compose's host ports** (8080/3000/9000/9010/9020/9090/3001, see `deploy/k8s/kind-config.yaml`) rather than picking fresh ones — so `NIMBUS_STORAGE_NODES`' `PublicEndpoint` half, `NEXT_PUBLIC_API_URL`'s build-time default, and anything a person has bookmarked all stay valid regardless of which environment is currently up. The trade-off is the two environments can never run concurrently; considered a non-issue for a local demo, not an oversight.
- **The nimbus-migrate image runs migrations via a Helm pre-install/pre-upgrade hook Job**, not baked into `nimbus-api`'s startup — keeps schema migration a deploy-time, one-shot concern independent of how many `nimbus-api` replicas come up, matching how `docker-compose.yml`'s `migrate` service already works. The hook Job inlines its own Postgres DSN rather than reading the chart's `nimbus-config` ConfigMap, because pre-install hooks run before the chart's other resources exist.

## Current status

All 3 weeks of the original roadmap plus the Day 15 buffer/DoD pass are done, and **the entire user-approved 15-item feature backlog is complete**: Tier 1 (#1–#6), Tier 2 (#7–#9), Tier 3 (#10–#13) — all 2026-07-05, items 17–20 above — and Tier 4 (#14–#15) on 2026-07-06 (item 21), plus user-requested extras along the way (the UX/sharing session; share-link clipboard auto-copy; folder/bundle zip downloads and the checkbox restyle; and the governance sessions — platform-admin gating + org usage, then the org-admin role tier + the Cluster/governance naming split, items 22–23). On top of the backlog, a full-codebase rated audit (2026-08-08) led to a modal-focus-trapping/keyboard-accessibility fix and an out-of-band discovery-and-fix of a stale `kind` deployment (item 26), a same-day fix for a broken Mermaid diagram render on GitHub in both `README.md` and `docs/02-system-design.md` (item 27), and — same day, user chose this over starting the VPS work — a "quick wins" pass through five of the audit's lower-effort roadmap items (item 28), then three further sessions closing every gap named in the audit's §01 "Architecture & module boundaries" (item 29: the `main.go` wiring split, `WriteQuorum`/`ChunkSizeBytes` enforcement, `internal/admin` discoverability), §02 "Distributed storage engine" (item 30: a real repair/re-replication pass, latency-aware placement, a cross-process health-disagreement alert), and §03/§04/§05 "Upload pipeline"/"Authentication & session security"/"Authorization & multi-tenancy" (item 31: streaming chunk reads + resume + download integrity, the httpOnly-cookie auth migration + JWT secret rotation + admin password complexity, and the membership cache + dedup proof-of-possession fix — see item 31 for the full list) sections. On top of that, four more sessions the same day worked through the audit's remaining roadmap items one at a time: item 32 closed roadmap #8 (toast-with-undo for trash/delete, `frontend/components/Toast.tsx`) and #13 (`helm lint`/`helm template`/kind smoke-deploy added to CI, both new steps live-verified with real `act` runs); item 33 closed roadmap #11 (`docs/openapi.json` generated from the real route table, `frontend/lib/types.ts` now derived from it, both directions enforced in CI); item 34 closed roadmap #14 (OpenTelemetry spans across HTTP → NATS → worker, Grafana Tempo added to both Compose and kind, live-verified via a real trace tree spanning both processes). Then item 35 (same day) closed the audit's three remaining low-priority polish rows together: #15 (computed real WCAG contrast ratios for `--muted`/`--muted-2` against every surface they render on — both already passed AA with margin, documented as a dated comment in `frontend/app/globals.css` rather than requiring a color change), #16 (a new shared `frontend/components/ui/EmptyState.tsx` now backs the new-org fallback, the empty-file folder view, both empty Trash lists, and the no-results search state — live-verified against a freshly registered account), and #17 (`backend/migrations/000004_fix_search_tokenization.down.sql` added, mirroring migration 000001's original tokenization so the sequence pairs up/down again). Of the audit's 17-item roadmap, only #5 (porting smoke scripts into the CI integration suite, High effort) remained open at this point — **closed by item 37, below**. Then item 36 (same day, user asked to close out the remaining §13 accessibility gaps and push that section's score to 9+) went beyond the roadmap table's own items: a real skip-to-content link (`frontend/components/AppShell.tsx`, jumps to and focuses a new `id="main-content"` on `<main>`); `aria-label`s distinguishing the desktop sidebar nav ("Primary") from the mobile bottom-tab nav ("Mobile"); a `prefers-reduced-motion` media query in `globals.css` that collapses all transitions/animations for anyone who's asked their OS for less motion; every `<label>`/`<input>` pair across the six auth-adjacent forms (login, register, forgot-password, reset-password, security/TOTP, the invite-member form) properly associated via matching `id`/`htmlFor` (previously visual-proximity-only, not programmatic — a real WCAG 1.3.1/4.1.2 failure, not a style nit); `role="alert"` added to all ~20 inline form-error messages across the app so screen reader users are actually told when a submission fails, not just sighted users; `role="status"` on the two auth flows whose success state silently replaces the form (forgot-password sent confirmation, reset-password done confirmation); and `aria-label`s on nine previously-nameless interactive elements found along the way (the file-row select checkbox, the inline file-rename input, the search box, the org/folder "create" inputs, the TOTP code inputs, the share-link expiry select and read-only link field, the invite-role select). All live-verified against a real running `kind` cluster: production build clean, typecheck/lint clean, the skip link keyboard-focuses and then click-activates to move real DOM focus onto `<main>`, both nav landmarks report their distinct names in the accessibility tree, the login form's password field now has a computed accessible name of "Password" (previously none), and a real failed-login attempt renders its error inside a `role="alert"` element. See docs/next-session.md for the full list. Then item 37 (2026-08-09, user said §14 Testing & Verification's 5.8/10 was too low and asked to fix/test everything) closed the audit's last open roadmap row (#5) and went well past it: real Go test files for the 7 backend modules that had zero coverage (`org`/`sharing`/`folder`/`file`/`gc`/`search`/`events`), a frontend test harness built from scratch (Vitest + Testing Library, 5 files/42 tests, previously zero), MinIO/NATS added to CI's `integration-test` job so `gc`/`events`/`sharing` are exercised on every push, and coverage reporting (no gate) on both languages — see item 37 for the four real bugs the new tests caught (a chunk-hash FK violation, `char(64)` column padding breaking equality checks, a pgx interval-binding type mismatch, and a stale-test-data matching bug) and exactly what's still untested (HTTP handlers backend-side, most of the frontend). Then item 38 (2026-08-09, same day as item 37 — user asked to close every gap in the audit's §16 "Docker, Compose & Kubernetes" and §18 "Repo hygiene & process" sections) closed all of both sections' fixable gaps: resource limits added everywhere they were missing (Compose, the Helm chart, and all seven k8s infra manifests, one sizing mistake caught by a live `kubectl top` check before anything was applied), the HPA stub turned into a real working autoscaler via a metrics-server install live-verified to flip `kubectl get hpa` from `<unknown>` to a real percentage, Compose's infra-only ports (Postgres/Redis/NATS/MinIO consoles/Mailpit/Tempo/Prometheus/Grafana) rebound to `127.0.0.1` instead of the previous `0.0.0.0`, a backup/restore story added for Postgres + all three MinIO nodes (catching and fixing a genuine Windows Docker-Desktop bind-mount-with-spaces bug along the way), and a new `CONTRIBUTING.md`. See item 38 for exactly what was and wasn't done (the orphaned Cloudflare tunnel Windows service was deliberately left for the user, not touched). Then item 39 (2026-08-09, same day) closed every fixable gap in the audit's §10 "Frontend architecture," §11 "Visual design system," and §12 "UX flows & interaction design" sections: real optimistic UI for rename/move/trash (SWR's `optimisticData`/`rollbackOnError`, live-verified with a real chunked upload and real UI clicks against the running kind cluster), a genuine second theme (light, contrast-audited the same way the dark-mode tokens already were, not a naive inversion, with a `useSyncExternalStore`-based toggle in both `AppShell` nav bars), the font-fallback chain confirmed and documented, a dev-only kitchen-sink route covering every `components/ui/` primitive, and direct tests for `lib/api.ts`'s refresh-race logic plus new `FileRow`/`UploadDropzone` component tests (frontend test count now 8 files/64 tests, up from 5/42). See item 39 for exactly what was and wasn't done (the optimistic-mutate implementation itself has live-browser verification and FileRow contract tests but no unit test mocking SWR's cache directly). See [docs/next-session.md](next-session.md) for the handoff — the go-public VPS plan (recipe in "Known issues" above) is still open and untouched across all of these sessions; it remains the next real piece of work, not any of the remaining roadmap items above.
