# Handoff — Next Session

Updated at the end of the frontend architecture/visual-design/UX session (2026-08-09, docs/00-project-state.md item 39 — same day as items 37 and 38, after the testing & verification session and the Docker/k8s hardening + repo hygiene session). Read [docs/00-project-state.md](00-project-state.md) first — it's the up-to-date source of truth; this file is about what to do next, not what's already true.

**Full-codebase rated audit (2026-08-08, docs/00-project-state.md items 26–27)**: https://claude.ai/code/artifact/b58f2813-ebc0-4213-94bd-62dbb9e4d8e8 — a published Artifact, not a repo file, so it's only reachable via this URL (private to the user's account; share it from the page's share menu if it ever needs to go to someone else). 19 aspects rated 0–10 with file-level evidence, plus a §19 prioritized roadmap table. **This URL was kept current through item 39** (republished, not re-minted, each time) — **all 17 roadmap rows are done** (closed by item 37), and every section-level "Gaps"/"Still worth knowing" list with a fixable item in it is now closed too: §13 Accessibility 9.0, §14 Testing & Verification 8.5 (item 37), §16 Docker/k8s 8.7 and §18 Repo hygiene 8.5 (item 38), §10 Frontend architecture/§11 Visual design/§12 UX flows all closed (item 39, this session — see item 39's own writeup and the republished artifact for the new scores). What's left below any section's ceiling is scope nobody named as a specific gap (§14's own remaining breadth, §16's shared dev credentials deliberately left for the VPS work) or genuinely open architectural choices, not overlooked fixes.

## What the frontend architecture/visual-design/UX session built (2026-08-09, docs/00-project-state.md item 39 — user asked to close every gap in §10/§11/§12)

See docs/00-project-state.md item 39 for the full writeup. Headline points:

- **§12 — optimistic UI for rename/move/trash.** `MoveDialog`'s `onMoved` became `onMove(targetFolderId): Promise<void>` — callers now own the mutation, not the dialog. `FolderPage` drives `mutate(promise, {optimisticData, rollbackOnError: true, revalidate: true, populateCache: false})`; `FileRow` calls three new props (`onOptimisticTrash`/`onOptimisticRename`/`onOptimisticMove`) instead of `api.files.*` directly. Trash failures surface via toast, not the row's own inline alert — the row can already be gone from the list (rolled back into a *fresh* instance) by the time a rejection arrives, so its local error state isn't a reliable place to show it; rename failures still use the inline alert since that row never leaves the list. Live-verified with a real chunked upload (browser-console API calls, no file-picker tool — same workaround item 26 used) and real UI clicks: renamed, trashed, and moved a real file, confirming both the instant UI update and the true server-side state via direct API checks each time.
- **§11 — a real light theme, not a naive inversion.** `globals.css` gained a full light token set (`prefers-color-scheme: light` + `[data-theme]` override, resolution order documented at the top of the file). `--accent`/`--accent-strong` are unchanged across themes (fills/rings, already pass 3:1 on light); `--accent-2`/`--danger`/`--success`/`--warning` were genuinely re-picked since they're body-text/link color and the dark values fail outright on white (as low as ~1.7:1) — every light token's worst-case contrast is documented the same way the existing dark-mode AA comment already was. `lib/theme.ts` + a `beforeInteractive` script avoid a theme-flash on load; `ThemeToggle` uses `useSyncExternalStore` (not `useState`+`useEffect`, which this repo's own `react-hooks/set-state-in-effect` lint rule flagged) and lives in both `AppShell` nav bars. Live-verified: toggled it, confirmed every token flipped via computed styles, confirmed `localStorage` persistence survived a real reload with no flash.
- **§11 — font fallback confirmed and documented** (it already worked — Next's font loader self-hosts + metrics-matches a fallback, and `globals.css` already chained `ui-sans-serif, system-ui, sans-serif` after it — the gap was that nobody had checked/written it down). Verified live by overriding `--font-mona-sans` to an unavailable name in devtools and confirming the computed font-family actually falls through.
- **§11 — a dev-only kitchen-sink route** (`app/dev/kitchen-sink/`, the section's own suggested fix) rendering every `components/ui/` primitive in every state plus all 23 icons, gated by `NODE_ENV !== "development" → notFound()` (compiles, 404s in production, confirmed by `npm run build`).
- **§10 — direct tests for `lib/api.ts`** (`lib/api.test.ts`, new, 9 tests): the request/refresh-race/error-mapping logic named as this section's highest-value gap, including a regression test proving concurrent 401s share exactly one `refresh()` call rather than each rotating the same refresh token.
- **§10 — component tests for the file browser and upload entry point**: `FileRow.test.tsx` (new, 7 tests) and `UploadDropzone.test.tsx` (new, 6 tests) — this section's other named highest-value gap.
- **Frontend test count: 8 files / 64 tests, up from 5 files / 42** (item 37's baseline). `npx tsc --noEmit`/`npx eslint .`/`npm run build` all clean; `npx vitest run` — the full suite — green.
- **Not done, disclosed rather than silently skipped**: the optimistic-mutate implementation in `FolderPage` has live-browser verification and `FileRow`'s contract with it is tested, but no unit test mocks SWR's cache directly; §10's "ad hoc React Context, no single client-state store" observation was left alone — the audit's own note already called it "fine at this size," not a gap to fix.
- **Environment note**: a throwaway `theme-verify-<timestamp>@nimbus.dev` account, its auto-created org, a "Destination" subfolder, and one moved test file are sitting in the `nimbus` kind cluster's Postgres volume from the live verification above — not cleaned up, same disclosed-leftover pattern as `a11y-audit-test@nimbus.dev` (item 26) and `trace-test@nimbus.dev` (item 34).

## What the testing & verification session built (2026-08-09, docs/00-project-state.md item 37 — user said §14's 5.8/10 was too low and asked to fix/test everything)

See docs/00-project-state.md item 37 for the full writeup (all four real bugs the new tests caught, with fix detail). Headline points:

- **7 backend modules went from zero tests to real coverage**: `org`, `sharing`, `folder`, `file`, `gc`, `search`, `events` — a mix of white-box unit tests (pure logic: `org`'s membership cache/role guards, `search`'s cursor codec, `events`'s header carrier) and `//go:build integration` tests against real Postgres, plus real MinIO/NATS for `gc`/`events`. `gc`'s suite ports `scripts/smoke-gc.js`'s 13 assertions (minus the down-node container-kill scenario, still a manual/smoke concern) using SQL-backdated timestamps instead of real multi-minute waits. `sharing`'s suite ports the audit's other named smoke-script target. A new `upload` quota-enforcement integration test closes a gap that had no coverage anywhere, not even a smoke script.
- **Frontend test infrastructure built from scratch**: Vitest + React Testing Library + jsdom (previously zero devDependencies, zero config). 5 test files, 42 tests: `lib/sha256-stream.ts` (the hand-rolled streaming SHA-256 named in this file as untested intricate logic) verified against Node's real crypto as an oracle; `lib/upload.ts`'s resume/fingerprint state machine (the other named gap) tested through `uploadFile`'s observable behavior; `lib/format.ts`; `EmptyState` and `ConfirmDialog` component tests.
- **CI**: `integration-test` job gained real MinIO/NATS (`docker run -d` steps, not `services:` — GitHub Actions services can't pass the command args either image needs). Both languages' test steps now print a coverage summary (no threshold gate). `frontend` job gained an `npm test` step.
- **Real bugs caught by running the tests, not just writing them**: a chunk-hash FK violation (placeholder hashes need a real `chunks` row first), `char(64)` column space-padding breaking checksum equality assertions, a pgx `time.Duration`→`interval` type mismatch, and a stale-test-data matching bug in a DLQ test (fixed with a unique-per-run marker). Two Windows-specific Vitest environment bugs also hit and fixed: the default `forks` pool times out on this machine's spaces-containing path (switched to `threads`), and jsdom's `SubtleCrypto.digest` rejects `File.slice().arrayBuffer()`'s output via a cross-realm `instanceof` mismatch (stubbed in the one test file that needed it).
- **Verified for real**: the full `-tags=integration ./...` suite ran green and stable (3 repeated runs) against real throwaway Postgres/Redis/MinIO/NATS containers on non-default ports — the already-running `nimbus` kind cluster and `pulseboard-redis` were never touched. `act` validated `build-test` and `frontend` end-to-end for real (not dry-run); `integration-test` hit `act`'s pre-existing Docker-in-Docker limitation at the (unrelated, pre-existing) migrations step, so its new MinIO/NATS steps are unconfirmed on a real GitHub Actions runner until the next push — same caveat `helm-smoke-deploy` already carries.
- **Not done, disclosed rather than silently skipped**: HTTP handlers across all 7 newly-tested backend modules are still at 0% coverage — this closed the service/repository layer the audit's named targets (`sharing`, `gc`) live in, not every layer. Frontend coverage is real but narrow (~60% statements, only across the 6 files the new tests touch — most of `app/` and most components remain untested). No coverage threshold is enforced anywhere yet.

## What the roadmap #8/#13/#11/#14 sessions built (2026-08-08, docs/00-project-state.md items 32–34 — user asked to work through the audit's remaining roadmap items one at a time, then "all three, back-to-back")

See docs/00-project-state.md items 32–34 for the full writeups (verification detail per fix). Headline points:

- **Roadmap #8 — toast-with-undo.** New `frontend/components/Toast.tsx` (`ToastProvider`/`useToast`), wired into `FileRow.trash()` and `FolderPage.trashFolder()`. Deliberately not applied to the Trash page's permanent "Delete forever" — no restore endpoint exists for that, so no undo affordance was added there.
- **Roadmap #13 — Helm chart now validated in CI.** New `helm-smoke-deploy` job (`.github/workflows/ci.yml`): `helm lint`/`helm template` fast-fail, then a real `kind create` → build+load images → `apply.sh` → `helm install --wait` → curl health endpoints. **Not run end-to-end in GitHub Actions itself yet** (no push has landed) — locally verified as far as possible without disrupting the already-running `nimbus` kind cluster: `helm lint`/`template` for real, rendered manifests schema-checked via `kubectl apply --dry-run=server`, workflow YAML parses via `act -l`/`act -n`. If this job fails on its first real CI run, start by checking whether a GitHub-hosted runner's resource limits (2 CPU/7GB RAM) are tight for Postgres+3×MinIO+NATS+Prometheus+Grafana+3 app pods all on one kind node — that's the one thing that couldn't be verified locally.
- **Roadmap #11 — OpenAPI spec generated from real routes, frontend types derived from it.** New `backend/internal/apidoc` (DTOs) + `backend/cmd/openapi-gen` (AST-parses `cmd/api/*.go`'s actual `mux.Handle` calls, so paths/methods/auth can't drift — schemas come from a hand-maintained per-route table, which *can* go stale, but the generator hard-fails if a real route has no table entry). Emits `docs/openapi.json`, checked in. `frontend/lib/api-schema.generated.ts` generated from it via `openapi-typescript` (new devDependency — pulled a transitive `js-yaml` CVE via `@redocly/openapi-core`, closed with an `overrides` pin in `frontend/package.json`). `frontend/lib/types.ts` rewritten from a ~170-line hand-maintained file into a ~55-line re-export layer — same exported names, so nothing else changed. **Regenerate both after touching a route or an `internal/apidoc` DTO**: `make openapi` (or the two commands in that target). CI fails on staleness in either direction (`build-test`'s "openapi spec is up to date", `frontend`'s "generated api types are up to date") — both verified with real `act` runs, not dry-runs.
- **Roadmap #14 — OpenTelemetry tracing, Grafana Tempo backend.** New `backend/internal/platform/tracing` (SDK setup, optional via `NIMBUS_OTEL_EXPORTER_ENDPOINT`), HTTP middleware (`httpserver.Tracing`), and NATS header-based context propagation (`internal/events`) so a trace continues from the originating HTTP request into `nimbus-worker`'s processing spans (`internal/processing`: `reassemble_chunks`/`generate_thumbnail`/`store_thumbnail`). Tempo added to Compose (`tempo` service, `deploy/observability/tempo.yaml`) and kind (`deploy/k8s/infra/tempo.yaml`, ClusterIP-only) plus a Grafana datasource. **Live-verified for real**: brought up a Compose stack (kind briefly stopped for the port window, same pattern as prior sessions, restarted after), ran the existing `scripts/smoke-thumbnails.js` unmodified, then queried Tempo's own API and confirmed one trace tree spanning both processes with correct parent/child spans — see item 34 for the exact trace IDs and shape.
- **A root `Makefile` target was added**: `make openapi` regenerates both the spec and the frontend types in one step.

## What the roadmap #15/#16/#17 session built (2026-08-08, docs/00-project-state.md item 35 — user asked to close the audit's last three UI/UX polish rows)

- **Roadmap #15 — WCAG contrast audit.** Computed real contrast ratios (not eyeballed) for `--muted`/`--muted-2` against every surface they actually render on (`--background`/`--surface`/`--surface-deep`/`--surface-2`, via a small Node script): `--muted` is 8.5–9.7:1 everywhere, `--muted-2`'s worst case is 4.77:1 on `--surface-2` (best case 5.44:1 on `--background`). Both already cleared AA's 4.5:1 for normal text with real margin — **no color change was needed**. The result is now a dated comment in `frontend/app/globals.css` next to the token definitions, so it doesn't need re-deriving and flags itself if either token or a new surface shade changes without re-checking.
- **Roadmap #16 — designed empty states.** New `frontend/components/ui/EmptyState.tsx` (icon well + title + optional description/action) replaced four bare one-line empty states: the search page's no-results state, both of the Trash page's folders/files lists, and the org-root "no folders yet" fallback (now built on the same component, with the existing create-folder form as its action). The folder page's already-icon'd empty-file state was migrated onto the shared component too, for consistency rather than a fifth one-off. **Live-verified**: registered a genuinely fresh account/org against the running `kind` cluster (`npm run dev` on an auto-picked port, pointed at kind's `localhost:8080` API per `.env.local`), confirmed the auto-created "Home" folder's empty-file state, then hit the search page's no-results state with a real query and both of the Trash page's empty lists — all render the new copy/icon with no console errors.
- **Roadmap #17 — missing migration down-file.** `backend/migrations/000004_fix_search_tokenization.down.sql` added, mirroring `000001_init.up.sql`'s original `to_tsvector('simple', name)` definition of `name_tsv` — rolling back past 000004 now restores the pre-fix tokenization instead of erroring on a missing file.
- **The audit artifact was republished** (same URL, see above) with a 7th revision block, updated scorecard/roadmap-table rows, and section-level note updates in §06/§11/§12/§13 reflecting all three closed.
- **Not done**: roadmap #5 (porting smoke scripts into CI) — still the sole open row. No automated test coverage was added for the new `EmptyState` component.

## What the accessibility-hardening session built (2026-08-08, docs/00-project-state.md item 36 — user said §13's 6.8→7.0 was still too low and asked to close every remaining gap, target 9+)

- **Skip-to-content link** (`frontend/components/AppShell.tsx`): a new `<a href="#main-content" class="skip-link">` is the first focusable element on every authenticated page, visually hidden until keyboard-focused (`.skip-link` in `globals.css`). `<main>` gained `id="main-content" tabIndex={-1}` so activating the link actually moves DOM focus, not just the viewport. **Live-verified**: Tab from a fresh page load lands on the link first; clicking it sets `location.hash` to `#main-content` and `document.activeElement` to the `<main>` element.
- **Nav landmarks now have distinct names**: the desktop sidebar `<nav>` is `aria-label="Primary"`, the mobile bottom-tab `<nav>` is `aria-label="Mobile"` — matters if both are ever in the tree at once (browser zoom/reflow edge cases), and is what an automated audit tool checks for regardless. Confirmed both names show up correctly in a live accessibility-tree read.
- **`prefers-reduced-motion: reduce` is now honored globally** (`globals.css`) — collapses all `transition`/`animation` durations to near-zero for anyone who's set that OS preference, rather than leaving every `transition-colors`/backdrop-blur untouched.
- **Every auth-adjacent form's `<label>`/`<input>` pairs are now programmatically associated** via matching `id`/`htmlFor` — previously visual-proximity-only (a `<label>` and `<Input>` as plain siblings, no `for`/`id`), which is a real WCAG 1.3.1/4.1.2 failure: a screen reader focusing the input wouldn't announce its label, and clicking the label text wouldn't focus the input. Fixed in login (email/password/TOTP-code), register (email/password/org-name), forgot-password (email), reset-password (new password), the members invite form. **Live-verified**: `document.getElementById('login-email')` plus `document.querySelector('label[for="login-email"]')` both resolve and match, and the accessibility tree now reports the login password field's name as "Password" where it previously had none.
- **`role="alert"` added to all ~20 inline form-error `<p>`/`<span>` elements app-wide** (login ×2, register, forgot-password, reset-password, security, members, search, org-root, folder, app-root, MoveDialog, FileRow, ShareDialog, ConfirmDialog, admin DLQ error, the three error spots on the public share page) — previously a failed submission was silent to a screen reader unless the user happened to navigate to where the error text landed. **Live-verified**: a real failed login (`nonexistent@nimbus.dev` / wrong password) renders its error inside a `[role="alert"]` element with the exact server message.
- **`role="status"` added to the two auth flows whose success state silently replaces the form**: forgot-password's "check your inbox" confirmation, reset-password's "password updated" confirmation.
- **`aria-label` added to nine previously-nameless interactive elements** found while doing this pass, not named in the original audit: the file-row select-for-sharing checkbox (the real `<input>` was wrapped in a `<label>` with only a `title`, which does **not** give the input an accessible name — a genuine gap, not a nitpick), the inline file-rename `<input>`, the main search box, the org-root and in-folder "create folder" inputs, the two TOTP code inputs (confirm-enrollment and confirm-disable), the share-dialog's expiry `<select>` and read-only link `<input>`, and the members invite form's role `<select>`.
- **Not done**: still no automated axe/Lighthouse run (this was a manual, code-level pass covering the WCAG criteria those tools check most — labels, landmarks, live regions, motion, contrast from item 35 — but a real tool run could still surface more), and still zero frontend test coverage for any of this (§10/§14 unchanged, consistent with every prior session).
- **The audit artifact was republished** (same URL) — §13 Accessibility moved 6.8/7.0 → 9.0, with the "still missing" list narrowed to just the automated-tooling gap; the overall score and masthead revision count were bumped to match.
- **Verification**: `npx tsc --noEmit` clean, `npx eslint .` clean (one pre-existing unrelated warning), `npm run build` clean (all 20 routes compiled), plus the live browser checks described above against the same `kind` cluster used for item 35 — no new Compose/kind cycle needed since this was a pure frontend-code change verified via the standalone `npm run dev` pattern.

## What the §03/§04/§05 audit-fix session built (2026-08-08, item 31 — user asked to fix every gap named in all three sections)

See docs/00-project-state.md item 31 for the full writeup (all eight gaps, with verification detail per fix). Headline points:

- **Roadmap #1 is done**: the refresh token is off `localStorage`, now an `httpOnly`/`SameSite=Strict` cookie behind a thin Next.js BFF proxy (`app/api/auth/*`). This was the audit's single highest-leverage remaining item — don't re-suggest it.
- **Dedup proof-of-possession is done**: a new `org_chunk_proofs` table (migration 000014) means an org can no longer attach to another org's content via a leaked chunk hash alone — confirmed exploitable pre-fix and confirmed closed post-fix via a live two-org attack reproduction. Also don't re-suggest this one.
- Upload streaming/resume/download-integrity (§03), JWT secret rotation + admin password complexity (§04 alongside the cookie fix), and a short-TTL membership cache (§05) rounded out the other five gaps.
- **A real bug was caught by live testing, not code review**: the initial httpOnly-cookie implementation had `AuthProvider`'s bootstrap call and `request()`'s 401-retry path both calling `refresh()` without sharing the in-flight-promise dedup guard — React Strict Mode's double-invoked mount effect fired two concurrent refreshes on every page load in dev, and the loser's reuse of an already-rotated token revoked the user's own session. Fixed same session; worth remembering as a pattern (any code path that can call a token-rotating function needs to share ONE dedup guard, not one per call site).
- `/v1/chunks/check` no longer exists — it moved to `/v1/uploads/{uploadId}/chunks/check` (org-scoped, needed for the proof-of-possession fix). Anything that still references the old route (a stray script, a bookmark, muscle memory) needs updating.

## The 15-item feature backlog is COMPLETE, plus two governance sessions on top

**Tier 1 (#1–#6), Tier 2 (#7–#9), Tier 3 (#10–#13), and Tier 4 (#14–#15) are all done and verified live** — see docs/00-project-state.md items 17–21. Tier 4 (2026-07-06, item 21) closed the last two: #14 password reset (token table, Mailpit email, forgot/reset pages) and #15 TOTP 2FA (stdlib RFC 6238, two-step login, Security page), plus a user-requested share-link clipboard auto-copy. After that, two same-day governance sessions (items 22–23, user asked how org roles work, then approved both scopes) added platform-admin gating on `/v1/admin/*`, an owner/admin-gated org usage view, a three-tier org RBAC ladder (owner > admin > member), and a UI naming split ("Cluster" for infra vs. "Members" for org governance).

## What the quick-wins + two architecture-gap sessions built (2026-08-08, items 28–30 — user picked these over starting the VPS work)

All three same-day sessions, all live-verified — see docs/00-project-state.md items 28–30 for the full writeups. Headline points:

**Quick-wins (item 28)** — five of the audit's roadmap items:
- **Floating `:latest` tags pinned** across Compose + k8s infra for MinIO/Mailpit/Prometheus/Grafana.
- **`govulncheck` + `npm audit --audit-level=high` now run in CI**, and both scanners' real findings were fixed, not just gated: `x/text` bump, `toolchain go1.26.5` pin (crypto/tls ECH CVE), Next.js bumped to `16.3.0`.
- **Non-root `USER` in all four Dockerfiles**, verified running as the non-root uid with the web container still serving.
- **A dedicated, tighter login rate-limit bucket** (`NIMBUS_LOGIN_RATE_LIMIT_RPS`/`_BURST`, default 1rps/burst 5, IP-keyed) on `/v1/auth/login` + `/v1/auth/login/totp`, independent of the general limiter.
- **A real, working Grafana alert on the DLQ** — `nimbus_dlq_dead_events` (a scrape-time Postgres-backed `GaugeFunc`, deliberately not an in-process counter — see item 28 for why) feeds a provisioned alert rule that fires through Mailpit. Verified by actually inserting a dead event and watching the email land. **Compose-only** — kind has no alerting provisioning mounted and no Mailpit to deliver to.

**§01 architecture gaps (item 29)** — `cmd/api/main.go` split 518→170 lines into `adapters.go` + five `wire_*.go` concern files; `WriteQuorum`/`ChunkSizeBytes` config fields (previously loaded and never read) now really enforced in `upload.Service.CommitChunk` (409/413); `internal/admin` discoverability closed via package doc comments, no restructuring.

**§02 storage-engine gaps (item 30)** — `POST /v1/admin/nodes/{nodeId}/repair` (`internal/storage/repair.go`) actually builds the "manual re-replication script" docs/02-system-design.md had described since its original write-up but never had; `Router.Resolve` is now latency-tiered (`NIMBUS_STORAGE_SLOW_THRESHOLD`, default 200ms) so a technically-alive-but-slow node sinks behind faster ones; a new Grafana alert fires if `nimbus-api`/`nimbus-worker`'s independent health gauges genuinely disagree for 2+ minutes. Repair verified live with real simulated volume loss (delete-the-object-from-disk, repair, confirm restored); the latency tiering verified with three new unit tests in `internal/storage/router_test.go` rather than live latency injection (not practically reproducible here — see item 30's own caveat).

## Next objective: still the go-public VPS plan (untouched this session, same as the last several)

The one standing open thread. Full agreed recipe is in docs/00-project-state.md "Known issues" first bullet — don't re-derive it (VPS + Compose + Caddy/Let's Encrypt TLS in front of api **and** each MinIO node, DuckDNS subdomain, fresh secrets, narrowed CORS, `NEXT_PUBLIC_API_URL` set in Vercel and redeploy). **Still confirm the VPS IP + SSH key + provider with the user rather than assuming** — they provision the box themselves, this doesn't change that. All backend blockers (rate limiting, caps/quotas, DLQ visibility — Tier 2) and the pre-deployment audit (item 25 — seeded admin login, rate-limit/dockerignore fixes) are done. Item 38 (2026-08-09) closed the port-binding and resource-limit halves of the infra-exposure finding (Postgres/Redis/MinIO-console/Mailpit/Tempo/Prometheus/Grafana now `127.0.0.1`-only in Compose; real cpu/memory limits everywhere) — **what's left for this VPS work specifically is just the shared dev credentials** (`nimbus`/`nimbus`, `nimbus`/`nimbus-secret`), which item 38 deliberately didn't touch since they're real secrets to rotate at provisioning time, not a config-file fix. Remember to set the *real* `NIMBUS_ADMIN_PASSWORD` in the VPS's `.env` before its first boot (see the CLAUDE.md footgun — rotating it after the account already exists needs a manual delete-and-reseed, not just an env change; also now needs to satisfy the 12-char/3-of-4-classes complexity check added in item 31).

**The audit's 17-item roadmap is now fully closed** (item 37 shipped the last row, #5). If the user wants more testing/verification work instead of the VPS plan — item 37 disclosed real gaps, not a finished state: HTTP handlers across all 7 newly-tested backend modules (`org`/`sharing`/`folder`/`file`/`gc`/`search`/`events`) are still at 0% coverage; most of the frontend (`app/`, most components, `lib/download.ts`, `lib/live.ts`) has no tests; the down-node container-kill scenario from `scripts/smoke-gc.js` wasn't ported (still a manual/smoke concern); no coverage threshold is enforced in CI on either language, just a printed number. Also still open, never scoped/approved: mailpit-on-kind, a change-role endpoint (today role changes are remove + re-add). Treat all of this as suggestions to offer, not a backlog to burn down.

## What the two governance sessions built (2026-07-06, after Tier 4)

See docs/00-project-state.md items 22–23. Notes worth carrying:

- **Two kinds of "admin," deliberately separate**: `/v1/admin/*` is cluster ops (nodes/ring/DLQ), gated by `users.is_platform_admin` (migration 000012). `GET /v1/orgs/{orgId}/usage` and member management are org governance, gated by org role (admin tier and up). Don't collapse them — the nav item is named "Cluster" specifically to keep them apart.
- **Org roles are a three-tier ladder** (migration 000013): owner > admin > member, rank-compared in `org.RequireRole`. Admin bounds live in `org.Service`, not the UI: admins can only *grant* `member` (`ErrElevatedRoleNeedsOwner` otherwise) and only *remove* plain members (`ErrAdminRemovesMembersOnly` otherwise) — a delegated admin can never consolidate control. The creator stays unremovable by anyone. No change-role endpoint exists yet (today: remove + re-add).
- **Platform-admin promotion mechanism was replaced in the pre-deployment audit session (item 25)** — see below; `auth.Repository.PromotePlatformAdmins`/`NIMBUS_PLATFORM_ADMIN_EMAILS` no longer exist.
- **`GET /v1/auth/me` exists now** — the frontend uses it to hide the Cluster nav for non-admins and to make the Members page role-aware (invite form and remove buttons only shown to roles that could actually use them). Server-side checks are the real enforcement; `/me` is presentation only.
- **Usage privacy line** (documented in `org/usage.go`): aggregate action metadata only, all already member-visible via the activity feed; no file names/content, and explicitly not `auth_audit_log` (spans orgs, user-private).
- **Demo role ladder in Tier1 Demo Org** (platform-admin flag no longer applies to any of these — see below): `tier1-demo@nimbus.dev` / `tier1-demo-password` (org owner only), `org-admin-demo@nimbus.dev` / `org-admin-demo-pass` (org admin), `governance-demo@nimbus.dev` / `governance-demo-pass` (plain member). Log into each to see the Members/Cluster pages reshape by role.

## What the pre-deployment audit session built (2026-07-06, same day, before going public)

See docs/00-project-state.md item 25 for the full list. Notes worth carrying:

- **Platform-admin access is now a single seeded login, not an email list.** `NIMBUS_ADMIN_EMAIL`/`NIMBUS_ADMIN_PASSWORD` (required config, in `deploy/.env` for Compose and `values.yaml`/`secret.yaml` for Helm) are the only way to get `is_platform_admin=true` — `auth.Repository.EnsureSeededAdmin` creates that one account at boot if missing, or just ensures the flag if it already exists (still promote-only/idempotent, never touches the password on an existing account). The old `tier1-demo@nimbus.dev`/`test@gmail.com`/`test1@gmail.com` promotions were manually revoked in the dev DB as part of this change — those accounts are org-only now, not platform admins.
- **Two real security bugs fixed**: `ratelimit.ClientIP` now trusts the *last* `X-Forwarded-For` hop (not the first, which is attacker-controlled) — matters once Caddy is the reverse proxy in the go-public plan. Root `.dockerignore` now excludes `.env*` (carve-out for `.env.example`) — it previously let `Dockerfile.web`'s build stage copy `frontend/.env.local` (which had a live Vercel OIDC token) into a Docker layer.
- ~~**Known deliberate non-fix**: global chunk dedup (`upload.Service`) has no proof-of-possession check — a user who learns another org's chunk hash can reference it via dedup — flagged as a design tradeoff to decide on, not fixed.~~ **Fixed in item 31** (§03/§04/§05 audit-fix session) — see docs/00-project-state.md item 31. ~~The infra-exposure findings (Postgres/Redis/MinIO/Grafana/Prometheus all on `0.0.0.0` with weak shared dev credentials, no `restart:`/resource limits anywhere in `docker-compose.yml`)~~ — **port-binding and resource-limits halves fixed in item 38** (2026-08-09); the shared dev credentials themselves are still deliberately left for the go-public VPS work, since that's real-secret rotation, not a config fix.

## What the Tier 4 session built (2026-07-06, before the governance sessions)

See docs/00-project-state.md item 21 for the full list. Notes worth carrying:

- **TOTP is hand-rolled on the Go stdlib** (`internal/auth/totp.go`, ~70 lines) — deliberate, no OTP dependency; unit tests pin it to RFC 6238's Appendix B vectors. Don't swap in a library "for safety"; the vectors are the safety.
- **Replay guard**: every successful code verification burns its 30s time step in Redis (`nimbus:totp:used:<user>:<step>`, SetNX) — shared across login/confirm/disable. When testing flows back-to-back, use adjacent steps (±1 skew is accepted) or wait 30s; reusing the exact code you just used is *supposed* to fail.
- **Login challenge** is a 32-byte token hashed into Redis (`nimbus:totp:challenge:<sha256>`, 5-min TTL); a wrong code deliberately leaves it alive (retype), success consumes it. A pending (unconfirmed) enrollment does NOT gate login — only `confirmed_at IS NOT NULL` does.
- **Password reset revokes all refresh families in the same transaction** that consumes the token and rewrites the hash (`auth.Repository.ResetPassword`). Reset does not touch TOTP.
- **No SMTP configured → links are logged, not sent** (`auth.Service.ForgotPassword`); that's the kind/k8s behavior today since mailpit is Compose-only. Mailpit UI: http://localhost:8025, API `/api/v1/search?query=to:"<email>"`.
- **Clipboard auto-copy silently degrades**: `navigator.clipboard.writeText` throws when the document isn't focused (headless/automation) — the catch keeps the manual Copy button as fallback.
- Frontend gained `qrcode` (+`@types/qrcode`) for TOTP enrollment QR, and `jszip` for the public share pages' "Download folder"/"Download all" buttons (client-side zip, plans fetched just-in-time per file). Bundle zipping dedupes same-named files with a `(n)` suffix since a bundle has no folder structure to namespace them.
- The multi-select checkbox in the app's file browser now uses a custom `components/ui/Checkbox` — the raw native checkbox rendered as a stark white square unchecked (browser-default, unthemeable) against the dark UI. Reuse that component for any future checkbox rather than reintroducing the native look.

## Important context (carried forward, still true)

- **Verification pattern to keep using**: build, then check against the real thing — real API smokes (23 assertions for Tier 4, 21 for governance part 1, 15 for the role tier) plus full browser flows per role/perspective are the expected depth.
- **Known real gaps**: ~~HPA stub inert~~ fixed item 38 (metrics-server installed, `kubectl get hpa` reports real numbers). Helm chart in CI as of item 32 (not yet proven on a real GitHub Actions run). mailpit Compose-only, chaos/load/smoke scripts Compose-only. ~~Infra services (Postgres/Redis/MinIO/Grafana/Prometheus) still bind `0.0.0.0`... and no restart/resource policies~~ — port-binding and resource limits fixed item 38; weak shared dev credentials remain the VPS work's job specifically. Full list in docs/00-project-state.md.
- **Environment state as of the pre-deployment audit session's end**: full Compose stack up (`nimbus-web`, `mailpit` included) with images rebuilt from that session's code; migrations through 000013 applied. Demo users: see the role ladder above, plus `tier4-browser@nimbus.dev` / `tier4-browser-newpw` (**TOTP enabled** — logging in needs its authenticator secret, which only lives in that browser session's enrollment; if it's in the way, clear `user_totp` for that user in Postgres). ~~The Day 13 `kind` cluster node container remains stopped, not deleted.~~ **Corrected 2026-08-08 (docs/00-project-state.md item 26): this was wrong — the `nimbus` kind cluster had been running the whole time**, undocumented, and had drifted ~34 days stale (missing routes/fields) by the time anyone checked. It's now rebuilt and redeployed (helm release revision 3). Lesson for next time: don't trust this bullet's environment claims without a live check first — `docker ps` before assuming Compose vs. kind, or which one, is actually up.
- **Environment state as of the 2026-08-08 audit/accessibility session's end**: the `nimbus` kind cluster is up and fresh (helm revision 3, all four images rebuilt from current source including the `useModal` accessibility fix). Compose was not started this session. A throwaway test account (`a11y-audit-test@nimbus.dev`) and org ("A11y Test Org") were created against the kind cluster for live verification and were left in place, not cleaned up.
- **Environment state as of the 2026-08-08 quick-wins session's end**: Compose was brought up temporarily to verify this session's changes live (rate limiter, non-root Dockerfiles, the Grafana DLQ alert end-to-end through Mailpit), then torn down with `docker compose down` again (named volumes untouched, no data lost) — the `nimbus` kind cluster (which was up when this session started) was `docker stop`'d for that window and `docker start`'d back afterward, still on helm revision 3 from the prior session. An unrelated project's `pulseboard-redis` container (port 6379) was also briefly stopped/restarted during this window — unrelated to Nimbus, mentioned only in case its brief downtime matters to that other project.
- **Environment state as of the two 2026-08-08 architecture-gap sessions' end (items 29–30)**: same pattern repeated twice more — Compose brought up (rebuilt each time), verified live, torn down; kind and `pulseboard-redis` stopped for each window and restarted after. **kind is now meaningfully stale** relative to `main`'s actual code: it predates the `main.go` wiring split, the `WriteQuorum`/`ChunkSizeBytes` enforcement, the storage repair endpoint, latency-aware placement, and both new Grafana alert rules — none of that has ever run on kind. Rebuild all four images + `kind load` + `helm upgrade` + `rollout restart` before trusting kind for anything touching those (same drift pattern item 26 already caught once; still no CI guard against it — see the "Helm chart isn't in CI" gap).
- **Environment state as of the §03/§04/§05 audit-fix session's end (item 31)**: same pattern again — Compose brought up (rebuilt, migration 000014 applied), verified live via `curl` + a real browser + the full smoke suite, torn down with `docker compose down` (volumes untouched); kind and `pulseboard-redis` stopped for the window and restarted after. **kind is now even more stale** — add migration 000014 (`org_chunk_proofs`), the BFF auth routes, and everything else in item 31 to the "hasn't run on kind" list above. Two environment issues hit, both pre-existing and unrelated to this session's code: the `nimbus-redis-1` empty-network-attachment glitch recurred (same force-recreate fix as before) and `deploy/.env`'s `NIMBUS_STORAGE_NODES` still pointed at dead Cloudflare Quick Tunnel hostnames from 2026-07-06/07 (commented out — **this is a local, gitignored file change that persists**; re-enable with fresh hostnames via `scripts/start-public-tunnels.ps1` if the public-tunnel setup is wanted again). `.claude/launch.json` (also gitignored) gained `nimbus-api`/`minio-node-{1,2,3}` `url`-only entries so the Browser preview tool can proxy those ports for live UI testing — keep them if doing more browser-based verification, they're harmless otherwise.
- **Environment state as of the roadmap #14 (Tempo) session's end (item 34)**: same Compose-up/verify/teardown/kind-restart pattern again, this time including the new `tempo` service — `docker compose down` without `-v`, so the `pgdata`/`minio1-3`/**`tempo-data`** (new) named volumes all persist for next time; a throwaway `trace-test@nimbus.dev` / "Trace Test Org" account plus one small uploaded PNG are sitting in that Postgres volume, not cleaned up (same disclosed-leftover-test-data pattern as `a11y-audit-test@nimbus.dev` in an earlier session). `pulseboard-redis`'s host port (6379) conflicted with Compose's own `redis` this time — worked around with a **local-only, not-committed** Compose override (`ports: !reset []` on the `redis` service) rather than touching the unrelated `pulseboard-redis` container; no override file was left behind. kind was `docker stop`'d/`docker start`'d for the window as usual and confirmed fully `Running` again afterward — it's now stale relative to `main` by everything in items 32–34 on top of the item-31-era staleness already noted above (roadmap #13's new `helm-smoke-deploy` CI job would catch exactly this kind of drift on every push once it's actually exercised by a real push — see that item's "not yet run in GitHub Actions" note).
- **Environment state as of the roadmap #15/#16/#17 session's end (item 35)**: unlike prior sessions, Compose was never brought up — verification ran `npm run dev` directly (frontend-only, per CLAUDE.md's "skip nimbus-web and iterate on the frontend directly" pattern) against the already-running `kind` cluster's API (`localhost:8080`, per `frontend/.env.local`'s `NEXT_PUBLIC_API_URL`), on an auto-picked port since kind already holds 3000. kind itself was never stopped/restarted this session — still on helm revision 3, now stale by everything in items 32–35 (the frontend changes here were verified via the standalone dev server, not by rebuilding/reloading the kind image). A throwaway `empty-state-audit@nimbus.dev` account and its auto-created "Home" org/folder are sitting in kind's Postgres volume, not cleaned up (same disclosed-leftover pattern as earlier sessions' test accounts).
- **Environment state as of the testing & verification session's end (item 37)**: no Compose stack and no kind changes this session — all new backend integration tests ran against **standalone throwaway containers** on non-default ports (`nimbus-test-postgres`:15432, `nimbus-test-redis`:16379, `nimbus-test-minio`:19000, `nimbus-test-nats`:14222), all four `docker rm -f`'d at the end of the session; the already-running `nimbus` kind cluster (still helm revision 3, now stale by everything in items 32–37 — nothing in this session ran against it) was never touched. `act` runs for CI validation briefly needed port 6379 free: `pulseboard-redis` was `docker stop`'d for one `act -j integration-test` run and `docker start`'d back immediately after (same pattern items 28/29–30/31/34 already used) — confirmed healthy again before ending the session. Leftover local-only files: `frontend/vitest.config.mts`, `frontend/vitest.setup.ts`, and the new `*.test.ts`/`*.test.tsx` files are all checked-in, not gitignored/throwaway.
- **Environment state as of the Docker/k8s hardening session's end (item 38)**: unlike item 37, this session *did* touch the live `nimbus` kind cluster — `kubectl apply`'d the new `metrics-server.yaml` and the resource-limited infra manifests, then `helm upgrade`'d (now revision 4). That upgrade only changed pod *specs* (resources.limits) via the same unchanged `:latest` image tags (`pullPolicy: Never`) — it did **not** rebuild/reload images, so the cluster is still running the same application code as before (still stale relative to `main` by everything in items 32–37 that was never `kind load`'d in) — just with the new resource limits, metrics-server, and infra hardening now live on top of that stale code. All rollouts confirmed healthy (`kubectl rollout status` clean, zero unexpected restarts) and `scripts/smoke-auth.sh` passed against the post-upgrade cluster. Compose was never brought up this session (validated via `docker compose config` only, to avoid the Compose/kind port conflict) — its resource-limit and port-binding changes are unverified against a live Compose stack until one is next started.
- A git remote is configured (`origin` → `github.com/Ritesh956/nimbus-storage-platform`, private). Commit and push at the end of each session, confirming with the user before each push. Branch protection is live on `main` (4 CI jobs required for PR merges; direct pushes still work).

## Warnings

- Docker Desktop on this machine has previously had a stale-socket issue after being killed/reinstalled; a one-off `TLS handshake timeout` pulling a new image was fixed with a direct `docker pull` retry.
- A native Windows PostgreSQL 17 install competes with Docker Desktop for host port 5432 — run integration tests inside the compose network (see CLAUDE.md footguns).
- `kind`/`helm`/`act`/`gh` (and `go` itself in fresh shells) aren't reliably on `PATH` — `go` lives at `C:\Program Files\Go\bin\go.exe`.
- Compose and kind can't run simultaneously (same host ports). Check what's actually up before assuming.
